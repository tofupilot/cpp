// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <chrono>
#include <optional>
#include <sstream>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "config.hpp"
#include "detail.hpp"
#include "error.hpp"
#include "types.hpp"

namespace tofupilot {

class TofuPilot;

class RevisionsClient {
public:
    explicit RevisionsClient(TofuPilot& client) noexcept : client_(client) {}

    class GetBuilder;
    GetBuilder get();
    class DeleteBuilder;
    DeleteBuilder delete_();
    class UpdateBuilder;
    UpdateBuilder update();
    class CreateBuilder;
    CreateBuilder create();

private:
    TofuPilot& client_;
};

class RevisionsClient::GetBuilder {
public:
    explicit GetBuilder(TofuPilot& client) noexcept : client_(client) {}

    GetBuilder& part_number(std::string value) {
        part_number_ = std::move(value);
        return *this;
    }
    GetBuilder& revision_number(std::string value) {
        revision_number_ = std::move(value);
        return *this;
    }

    GetBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    GetBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    PartGetRevisionResponse send() {
        if (!part_number_.has_value()) throw ValidationError("missing required path parameter: part_number");
        if (!revision_number_.has_value()) throw ValidationError("missing required path parameter: revision_number");

        std::string path = "/v2/parts/" + detail::url_encode(part_number_.value()) + "/revisions/" + detail::url_encode(revision_number_.value()) + "";

        std::string query_string;

        std::string body_str;
        std::string content_type;

        auto result = client_.execute("GET", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return PartGetRevisionResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<PartGetRevisionResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> part_number_;
    std::optional<std::string> revision_number_;
    RequestConfig request_config_;
};

class RevisionsClient::DeleteBuilder {
public:
    explicit DeleteBuilder(TofuPilot& client) noexcept : client_(client) {}

    DeleteBuilder& part_number(std::string value) {
        part_number_ = std::move(value);
        return *this;
    }
    DeleteBuilder& revision_number(std::string value) {
        revision_number_ = std::move(value);
        return *this;
    }

    DeleteBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    DeleteBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    PartDeleteRevisionResponse send() {
        if (!part_number_.has_value()) throw ValidationError("missing required path parameter: part_number");
        if (!revision_number_.has_value()) throw ValidationError("missing required path parameter: revision_number");

        std::string path = "/v2/parts/" + detail::url_encode(part_number_.value()) + "/revisions/" + detail::url_encode(revision_number_.value()) + "";

        std::string query_string;

        std::string body_str;
        std::string content_type;

        auto result = client_.execute("DELETE", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return PartDeleteRevisionResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<PartDeleteRevisionResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> part_number_;
    std::optional<std::string> revision_number_;
    RequestConfig request_config_;
};

class RevisionsClient::UpdateBuilder {
public:
    explicit UpdateBuilder(TofuPilot& client) noexcept : client_(client) {}

    UpdateBuilder& part_number(std::string value) {
        part_number_ = std::move(value);
        return *this;
    }
    UpdateBuilder& revision_number(std::string value) {
        revision_number_ = std::move(value);
        return *this;
    }
    UpdateBuilder& number(std::string value) {
        number_ = std::move(value);
        return *this;
    }
    UpdateBuilder& image_id(std::string value) {
        image_id_ = std::move(value);
        return *this;
    }
    UpdateBuilder& body(PartUpdateRevisionRequestBody b) {
        if (b.number.has_value()) number_ = std::move(b.number);
        if (b.image_id.has_value()) image_id_ = std::move(b.image_id);
        return *this;
    }

    UpdateBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    UpdateBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    PartUpdateRevisionResponse send() {
        if (!part_number_.has_value()) throw ValidationError("missing required path parameter: part_number");
        if (!revision_number_.has_value()) throw ValidationError("missing required path parameter: revision_number");

        std::string path = "/v2/parts/" + detail::url_encode(part_number_.value()) + "/revisions/" + detail::url_encode(revision_number_.value()) + "";

        std::string query_string;

        std::string body_str;
        std::string content_type;
        {
            PartUpdateRevisionRequestBody req_body;
            req_body.number = number_;
            req_body.image_id = image_id_;
            body_str = nlohmann::json(req_body).dump();
            content_type = "application/json";
        }

        auto result = client_.execute("PATCH", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return PartUpdateRevisionResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<PartUpdateRevisionResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> part_number_;
    std::optional<std::string> revision_number_;
    std::optional<std::string> number_;
    std::optional<std::string> image_id_;
    RequestConfig request_config_;
};

class RevisionsClient::CreateBuilder {
public:
    explicit CreateBuilder(TofuPilot& client) noexcept : client_(client) {}

    CreateBuilder& part_number(std::string value) {
        part_number_ = std::move(value);
        return *this;
    }
    CreateBuilder& number(std::string value) {
        number_ = std::move(value);
        return *this;
    }
    CreateBuilder& body(PartCreateRevisionRequestBody b) {
        number_ = std::move(b.number);
        return *this;
    }

    CreateBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    CreateBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    PartCreateRevisionResponse send() {
        if (!part_number_.has_value()) throw ValidationError("missing required path parameter: part_number");

        std::string path = "/v2/parts/" + detail::url_encode(part_number_.value()) + "/revisions";

        std::string query_string;

        std::string body_str;
        std::string content_type;
        {
            PartCreateRevisionRequestBody req_body;
            if (!number_.has_value()) throw ValidationError("missing required field: number");
            req_body.number = number_.value();
            body_str = nlohmann::json(req_body).dump();
            content_type = "application/json";
        }

        auto result = client_.execute("POST", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return PartCreateRevisionResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<PartCreateRevisionResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> part_number_;
    std::optional<std::string> number_;
    RequestConfig request_config_;
};

inline RevisionsClient::GetBuilder RevisionsClient::get() {
    return GetBuilder(client_);
}
inline RevisionsClient::DeleteBuilder RevisionsClient::delete_() {
    return DeleteBuilder(client_);
}
inline RevisionsClient::UpdateBuilder RevisionsClient::update() {
    return UpdateBuilder(client_);
}
inline RevisionsClient::CreateBuilder RevisionsClient::create() {
    return CreateBuilder(client_);
}

} // namespace tofupilot
