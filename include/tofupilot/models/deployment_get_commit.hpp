// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/deployment_get_branch.hpp"
#include "tofupilot/models/deployment_get_repository.hpp"

namespace tofupilot {

/// Git commit the deployment was built from. Null for non-git deployments.
struct DeploymentGetCommit {
    std::string id;
    std::string sha;
    std::string message;
    std::string author_username;
    std::optional<std::string> author_avatar_url;
    std::optional<std::string> committed_at;
    std::optional<DeploymentGetBranch> branch;
    std::optional<DeploymentGetRepository> repository;
};

inline void to_json(nlohmann::json& j, const DeploymentGetCommit& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    j["sha"] = v.sha;
    j["message"] = v.message;
    j["author_username"] = v.author_username;
    if (v.author_avatar_url.has_value()) {
        j["author_avatar_url"] = v.author_avatar_url.value();
    }
    if (v.committed_at.has_value()) {
        j["committed_at"] = v.committed_at.value();
    }
    if (v.branch.has_value()) {
        j["branch"] = v.branch.value();
    }
    if (v.repository.has_value()) {
        j["repository"] = v.repository.value();
    }
}

inline void from_json(const nlohmann::json& j, DeploymentGetCommit& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (!j.contains("sha")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: sha", &j);
    }
    v.sha = j["sha"].get<std::string>();
    if (!j.contains("message")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: message", &j);
    }
    v.message = j["message"].get<std::string>();
    if (!j.contains("author_username")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: author_username", &j);
    }
    v.author_username = j["author_username"].get<std::string>();
    if (j.contains("author_avatar_url") && !j["author_avatar_url"].is_null()) {
        v.author_avatar_url = j["author_avatar_url"].get<std::string>();
    }
    if (j.contains("committed_at") && !j["committed_at"].is_null()) {
        v.committed_at = j["committed_at"].get<std::string>();
    }
    if (j.contains("branch") && !j["branch"].is_null()) {
        v.branch = j["branch"].get<DeploymentGetBranch>();
    }
    if (j.contains("repository") && !j["repository"].is_null()) {
        v.repository = j["repository"].get<DeploymentGetRepository>();
    }
}

/// Builder for DeploymentGetCommit.
class DeploymentGetCommitBuilder {
public:
    /// Set the `id` field.
    DeploymentGetCommitBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    /// Set the `sha` field.
    DeploymentGetCommitBuilder& sha(std::string value) {
        sha_ = std::move(value);
        return *this;
    }

    /// Set the `message` field.
    DeploymentGetCommitBuilder& message(std::string value) {
        message_ = std::move(value);
        return *this;
    }

    /// Set the `author_username` field.
    DeploymentGetCommitBuilder& author_username(std::string value) {
        author_username_ = std::move(value);
        return *this;
    }

    /// Set the `author_avatar_url` field.
    DeploymentGetCommitBuilder& author_avatar_url(std::string value) {
        author_avatar_url_ = std::move(value);
        return *this;
    }

    /// Set the `committed_at` field.
    DeploymentGetCommitBuilder& committed_at(std::string value) {
        committed_at_ = std::move(value);
        return *this;
    }

    /// Set the `branch` field.
    DeploymentGetCommitBuilder& branch(DeploymentGetBranch value) {
        branch_ = std::move(value);
        return *this;
    }

    /// Set the `repository` field.
    DeploymentGetCommitBuilder& repository(DeploymentGetRepository value) {
        repository_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    DeploymentGetCommit build() const& {
        DeploymentGetCommit result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = id_.value();
        if (!sha_.has_value()) {
            throw std::runtime_error("missing required field: sha");
        }
        result.sha = sha_.value();
        if (!message_.has_value()) {
            throw std::runtime_error("missing required field: message");
        }
        result.message = message_.value();
        if (!author_username_.has_value()) {
            throw std::runtime_error("missing required field: author_username");
        }
        result.author_username = author_username_.value();
        result.author_avatar_url = author_avatar_url_;
        result.committed_at = committed_at_;
        result.branch = branch_;
        result.repository = repository_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    DeploymentGetCommit build() && {
        DeploymentGetCommit result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = std::move(id_.value());
        if (!sha_.has_value()) {
            throw std::runtime_error("missing required field: sha");
        }
        result.sha = std::move(sha_.value());
        if (!message_.has_value()) {
            throw std::runtime_error("missing required field: message");
        }
        result.message = std::move(message_.value());
        if (!author_username_.has_value()) {
            throw std::runtime_error("missing required field: author_username");
        }
        result.author_username = std::move(author_username_.value());
        result.author_avatar_url = std::move(author_avatar_url_);
        result.committed_at = std::move(committed_at_);
        result.branch = std::move(branch_);
        result.repository = std::move(repository_);
        return result;
    }

private:
    std::optional<std::string> id_;
    std::optional<std::string> sha_;
    std::optional<std::string> message_;
    std::optional<std::string> author_username_;
    std::optional<std::string> author_avatar_url_;
    std::optional<std::string> committed_at_;
    std::optional<DeploymentGetBranch> branch_;
    std::optional<DeploymentGetRepository> repository_;
};

} // namespace tofupilot
