// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/nullable.hpp"
#include "tofupilot/models/batch_get_created_by_station.hpp"
#include "tofupilot/models/batch_get_created_by_user.hpp"
#include "tofupilot/models/batch_get_units.hpp"

namespace tofupilot {

/// Batch retrieved successfully
struct BatchGetResponse {
    /// Unique identifier for the batch.
    std::string id;
    /// Batch number.
    std::string number;
    /// ISO 8601 timestamp when the batch was created.
    std::string created_at;
    /// User who created this batch.
    NullableField<BatchGetCreatedByUser> created_by_user;
    /// Station that created this batch.
    NullableField<BatchGetCreatedByStation> created_by_station;
    /// Array of units in this batch. Empty array if no units.
    std::vector<BatchGetUnits> units;
};

inline void to_json(nlohmann::json& j, const BatchGetResponse& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    j["number"] = v.number;
    j["created_at"] = v.created_at;
    if (!v.created_by_user.is_absent()) {
        if (v.created_by_user.is_null()) {
            j["created_by_user"] = nullptr;
        } else {
            j["created_by_user"] = v.created_by_user.get();
        }
    }
    if (!v.created_by_station.is_absent()) {
        if (v.created_by_station.is_null()) {
            j["created_by_station"] = nullptr;
        } else {
            j["created_by_station"] = v.created_by_station.get();
        }
    }
    j["units"] = v.units;
}

inline void from_json(const nlohmann::json& j, BatchGetResponse& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (!j.contains("number")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: number", &j);
    }
    v.number = j["number"].get<std::string>();
    if (!j.contains("created_at")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: created_at", &j);
    }
    v.created_at = j["created_at"].get<std::string>();
    if (j.contains("created_by_user")) {
        if (j["created_by_user"].is_null()) {
            v.created_by_user = NullableField<BatchGetCreatedByUser>::make_null();
        } else {
            v.created_by_user = NullableField<BatchGetCreatedByUser>::value(j["created_by_user"].get<BatchGetCreatedByUser>());
        }
    }
    if (j.contains("created_by_station")) {
        if (j["created_by_station"].is_null()) {
            v.created_by_station = NullableField<BatchGetCreatedByStation>::make_null();
        } else {
            v.created_by_station = NullableField<BatchGetCreatedByStation>::value(j["created_by_station"].get<BatchGetCreatedByStation>());
        }
    }
    if (j.contains("units") && j["units"].is_array()) {
        v.units = j["units"].get<std::vector<BatchGetUnits>>();
    }
}

} // namespace tofupilot
