// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>


namespace tofupilot {

/// User who created the procedure.
struct ProcedureListCreatedByUser {
    /// Unique identifier for the user who created the procedure.
    std::string id;
    /// Name of the user who created the procedure.
    std::optional<std::string> name;
    /// Email of the user who created the procedure.
    std::string email;
};

inline void to_json(nlohmann::json& j, const ProcedureListCreatedByUser& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    if (v.name.has_value()) {
        j["name"] = v.name.value();
    }
    j["email"] = v.email;
}

inline void from_json(const nlohmann::json& j, ProcedureListCreatedByUser& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (j.contains("name") && !j["name"].is_null()) {
        v.name = j["name"].get<std::string>();
    }
    if (!j.contains("email")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: email", &j);
    }
    v.email = j["email"].get<std::string>();
}

/// Builder for ProcedureListCreatedByUser.
class ProcedureListCreatedByUserBuilder {
public:
    /// Set the `id` field.
    /// Unique identifier for the user who created the procedure.
    ProcedureListCreatedByUserBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    /// Set the `name` field.
    /// Name of the user who created the procedure.
    ProcedureListCreatedByUserBuilder& name(std::string value) {
        name_ = std::move(value);
        return *this;
    }

    /// Set the `email` field.
    /// Email of the user who created the procedure.
    ProcedureListCreatedByUserBuilder& email(std::string value) {
        email_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    ProcedureListCreatedByUser build() const& {
        ProcedureListCreatedByUser result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = id_.value();
        result.name = name_;
        if (!email_.has_value()) {
            throw std::runtime_error("missing required field: email");
        }
        result.email = email_.value();
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    ProcedureListCreatedByUser build() && {
        ProcedureListCreatedByUser result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = std::move(id_.value());
        result.name = std::move(name_);
        if (!email_.has_value()) {
            throw std::runtime_error("missing required field: email");
        }
        result.email = std::move(email_.value());
        return result;
    }

private:
    std::optional<std::string> id_;
    std::optional<std::string> name_;
    std::optional<std::string> email_;
};

} // namespace tofupilot
