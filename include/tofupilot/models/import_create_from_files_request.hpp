// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/import_create_from_files_items.hpp"

namespace tofupilot {

struct ImportCreateFromFilesRequest {
    /// Files to import (1–100). Pass a single-item list to import one file. Each item is parsed independently; one failure does not abort the others.
    std::vector<ImportCreateFromFilesItems> items;
};

inline void to_json(nlohmann::json& j, const ImportCreateFromFilesRequest& v) {
    j = nlohmann::json::object();
    j["items"] = v.items;
}

inline void from_json(const nlohmann::json& j, ImportCreateFromFilesRequest& v) {
    if (j.contains("items") && j["items"].is_array()) {
        v.items = j["items"].get<std::vector<ImportCreateFromFilesItems>>();
    }
}

/// Builder for ImportCreateFromFilesRequest.
class ImportCreateFromFilesRequestBuilder {
public:
    /// Set the `items` field.
    /// Files to import (1–100). Pass a single-item list to import one file. Each item is parsed independently; one failure does not abort the others.
    ImportCreateFromFilesRequestBuilder& items(std::vector<ImportCreateFromFilesItems> value) {
        items_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    ImportCreateFromFilesRequest build() const& {
        ImportCreateFromFilesRequest result;
        if (items_.has_value()) {
            result.items = items_.value();
        }
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    ImportCreateFromFilesRequest build() && {
        ImportCreateFromFilesRequest result;
        if (items_.has_value()) {
            result.items = std::move(items_.value());
        }
        return result;
    }

private:
    std::optional<std::vector<ImportCreateFromFilesItems>> items_;
};

} // namespace tofupilot
