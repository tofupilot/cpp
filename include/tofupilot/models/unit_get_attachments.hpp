// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>


namespace tofupilot {

struct UnitGetAttachments {
    /// Attachment ID.
    std::string id;
    /// File name.
    std::string name;
    /// File size in bytes.
    std::optional<double> size;
    /// MIME type of the file.
    std::optional<std::string> content_type;
    /// Presigned URL for downloading the file. This URL is temporary and will expire.
    std::optional<std::string> download_url;
};

inline void to_json(nlohmann::json& j, const UnitGetAttachments& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    j["name"] = v.name;
    if (v.size.has_value()) {
        j["size"] = v.size.value();
    }
    if (v.content_type.has_value()) {
        j["content_type"] = v.content_type.value();
    }
    if (v.download_url.has_value()) {
        j["download_url"] = v.download_url.value();
    }
}

inline void from_json(const nlohmann::json& j, UnitGetAttachments& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (!j.contains("name")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: name", &j);
    }
    v.name = j["name"].get<std::string>();
    if (j.contains("size") && !j["size"].is_null()) {
        v.size = j["size"].get<double>();
    }
    if (j.contains("content_type") && !j["content_type"].is_null()) {
        v.content_type = j["content_type"].get<std::string>();
    }
    if (j.contains("download_url") && !j["download_url"].is_null()) {
        v.download_url = j["download_url"].get<std::string>();
    }
}

/// Builder for UnitGetAttachments.
class UnitGetAttachmentsBuilder {
public:
    /// Set the `id` field.
    /// Attachment ID.
    UnitGetAttachmentsBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    /// Set the `name` field.
    /// File name.
    UnitGetAttachmentsBuilder& name(std::string value) {
        name_ = std::move(value);
        return *this;
    }

    /// Set the `size` field.
    /// File size in bytes.
    UnitGetAttachmentsBuilder& size(double value) {
        size_ = std::move(value);
        return *this;
    }

    /// Set the `content_type` field.
    /// MIME type of the file.
    UnitGetAttachmentsBuilder& content_type(std::string value) {
        content_type_ = std::move(value);
        return *this;
    }

    /// Set the `download_url` field.
    /// Presigned URL for downloading the file. This URL is temporary and will expire.
    UnitGetAttachmentsBuilder& download_url(std::string value) {
        download_url_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    UnitGetAttachments build() const& {
        UnitGetAttachments result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = id_.value();
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = name_.value();
        result.size = size_;
        result.content_type = content_type_;
        result.download_url = download_url_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    UnitGetAttachments build() && {
        UnitGetAttachments result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = std::move(id_.value());
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = std::move(name_.value());
        result.size = std::move(size_);
        result.content_type = std::move(content_type_);
        result.download_url = std::move(download_url_);
        return result;
    }

private:
    std::optional<std::string> id_;
    std::optional<std::string> name_;
    std::optional<double> size_;
    std::optional<std::string> content_type_;
    std::optional<std::string> download_url_;
};

} // namespace tofupilot
