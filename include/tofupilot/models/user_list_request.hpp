// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>


namespace tofupilot {

struct UserListRequest {
    /// If true, returns only the current authenticated user
    std::optional<bool> current;
};

inline void to_json(nlohmann::json& j, const UserListRequest& v) {
    j = nlohmann::json::object();
    if (v.current.has_value()) {
        j["current"] = v.current.value();
    }
}

inline void from_json(const nlohmann::json& j, UserListRequest& v) {
    if (j.contains("current") && !j["current"].is_null()) {
        v.current = j["current"].get<bool>();
    }
}

/// Builder for UserListRequest.
class UserListRequestBuilder {
public:
    /// Set the `current` field.
    /// If true, returns only the current authenticated user
    UserListRequestBuilder& current(bool value) {
        current_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    UserListRequest build() const& {
        UserListRequest result;
        result.current = current_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    UserListRequest build() && {
        UserListRequest result;
        result.current = std::move(current_);
        return result;
    }

private:
    std::optional<bool> current_;
};

} // namespace tofupilot
