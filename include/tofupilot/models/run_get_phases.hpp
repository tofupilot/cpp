// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/nullable.hpp"
#include "tofupilot/models/phases_outcome.hpp"
#include "tofupilot/models/run_get_measurements.hpp"

namespace tofupilot {

struct RunGetPhases {
    /// Phase ID.
    std::string id;
    /// Phase name.
    std::string name;
    /// Phase execution result.
    PhasesOutcome outcome;
    /// ISO 8601 timestamp when the phase started.
    std::string started_at;
    /// ISO 8601 timestamp when the phase ended.
    std::string ended_at;
    /// ISO 8601 duration of the phase (computed from started_at and ended_at).
    std::string duration;
    /// Zero-based retry attempt index. 0 = first attempt, 1 = first retry, etc.
    int64_t retry_count;
    /// Phase documentation string.
    NullableField<std::string> docstring;
    /// Array of measurements taken during this phase.
    std::vector<RunGetMeasurements> measurements;
};

inline void to_json(nlohmann::json& j, const RunGetPhases& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    j["name"] = v.name;
    j["outcome"] = v.outcome;
    j["started_at"] = v.started_at;
    j["ended_at"] = v.ended_at;
    j["duration"] = v.duration;
    j["retry_count"] = v.retry_count;
    if (!v.docstring.is_absent()) {
        if (v.docstring.is_null()) {
            j["docstring"] = nullptr;
        } else {
            j["docstring"] = v.docstring.get();
        }
    }
    j["measurements"] = v.measurements;
}

inline void from_json(const nlohmann::json& j, RunGetPhases& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (!j.contains("name")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: name", &j);
    }
    v.name = j["name"].get<std::string>();
    if (!j.contains("outcome")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: outcome", &j);
    }
    v.outcome = j["outcome"].get<PhasesOutcome>();
    if (!j.contains("started_at")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: started_at", &j);
    }
    v.started_at = j["started_at"].get<std::string>();
    if (!j.contains("ended_at")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: ended_at", &j);
    }
    v.ended_at = j["ended_at"].get<std::string>();
    if (!j.contains("duration")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: duration", &j);
    }
    v.duration = j["duration"].get<std::string>();
    if (!j.contains("retry_count")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: retry_count", &j);
    }
    v.retry_count = j["retry_count"].get<int64_t>();
    if (j.contains("docstring")) {
        if (j["docstring"].is_null()) {
            v.docstring = NullableField<std::string>::make_null();
        } else {
            v.docstring = NullableField<std::string>::value(j["docstring"].get<std::string>());
        }
    }
    if (j.contains("measurements") && j["measurements"].is_array()) {
        v.measurements = j["measurements"].get<std::vector<RunGetMeasurements>>();
    }
}

/// Builder for RunGetPhases.
class RunGetPhasesBuilder {
public:
    /// Set the `id` field.
    /// Phase ID.
    RunGetPhasesBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    /// Set the `name` field.
    /// Phase name.
    RunGetPhasesBuilder& name(std::string value) {
        name_ = std::move(value);
        return *this;
    }

    /// Set the `outcome` field.
    /// Phase execution result.
    RunGetPhasesBuilder& outcome(PhasesOutcome value) {
        outcome_ = std::move(value);
        return *this;
    }

    /// Set the `started_at` field.
    /// ISO 8601 timestamp when the phase started.
    RunGetPhasesBuilder& started_at(std::string value) {
        started_at_ = std::move(value);
        return *this;
    }

    /// Set the `ended_at` field.
    /// ISO 8601 timestamp when the phase ended.
    RunGetPhasesBuilder& ended_at(std::string value) {
        ended_at_ = std::move(value);
        return *this;
    }

    /// Set the `duration` field.
    /// ISO 8601 duration of the phase (computed from started_at and ended_at).
    RunGetPhasesBuilder& duration(std::string value) {
        duration_ = std::move(value);
        return *this;
    }

    /// Set the `retry_count` field.
    /// Zero-based retry attempt index. 0 = first attempt, 1 = first retry, etc.
    RunGetPhasesBuilder& retry_count(int64_t value) {
        retry_count_ = std::move(value);
        return *this;
    }

    /// Set the `docstring` field.
    /// Phase documentation string.
    RunGetPhasesBuilder& docstring(std::string value) {
        docstring_ = NullableField<std::string>::value(std::move(value));
        return *this;
    }

    /// Explicitly set `docstring` to null.
    RunGetPhasesBuilder& docstring_null() {
        docstring_ = NullableField<std::string>::make_null();
        return *this;
    }

    /// Set the `measurements` field.
    /// Array of measurements taken during this phase.
    RunGetPhasesBuilder& measurements(std::vector<RunGetMeasurements> value) {
        measurements_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    RunGetPhases build() const& {
        RunGetPhases result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = id_.value();
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = name_.value();
        if (!outcome_.has_value()) {
            throw std::runtime_error("missing required field: outcome");
        }
        result.outcome = outcome_.value();
        if (!started_at_.has_value()) {
            throw std::runtime_error("missing required field: started_at");
        }
        result.started_at = started_at_.value();
        if (!ended_at_.has_value()) {
            throw std::runtime_error("missing required field: ended_at");
        }
        result.ended_at = ended_at_.value();
        if (!duration_.has_value()) {
            throw std::runtime_error("missing required field: duration");
        }
        result.duration = duration_.value();
        if (!retry_count_.has_value()) {
            throw std::runtime_error("missing required field: retry_count");
        }
        result.retry_count = retry_count_.value();
        result.docstring = docstring_;
        if (measurements_.has_value()) {
            result.measurements = measurements_.value();
        }
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    RunGetPhases build() && {
        RunGetPhases result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = std::move(id_.value());
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = std::move(name_.value());
        if (!outcome_.has_value()) {
            throw std::runtime_error("missing required field: outcome");
        }
        result.outcome = std::move(outcome_.value());
        if (!started_at_.has_value()) {
            throw std::runtime_error("missing required field: started_at");
        }
        result.started_at = std::move(started_at_.value());
        if (!ended_at_.has_value()) {
            throw std::runtime_error("missing required field: ended_at");
        }
        result.ended_at = std::move(ended_at_.value());
        if (!duration_.has_value()) {
            throw std::runtime_error("missing required field: duration");
        }
        result.duration = std::move(duration_.value());
        if (!retry_count_.has_value()) {
            throw std::runtime_error("missing required field: retry_count");
        }
        result.retry_count = std::move(retry_count_.value());
        result.docstring = std::move(docstring_);
        if (measurements_.has_value()) {
            result.measurements = std::move(measurements_.value());
        }
        return result;
    }

private:
    std::optional<std::string> id_;
    std::optional<std::string> name_;
    std::optional<PhasesOutcome> outcome_;
    std::optional<std::string> started_at_;
    std::optional<std::string> ended_at_;
    std::optional<std::string> duration_;
    std::optional<int64_t> retry_count_;
    NullableField<std::string> docstring_;
    std::optional<std::vector<RunGetMeasurements>> measurements_;
};

} // namespace tofupilot
