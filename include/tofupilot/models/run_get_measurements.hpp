// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/nullable.hpp"
#include "tofupilot/models/run_get_aggregations.hpp"
#include "tofupilot/models/run_get_data_series.hpp"
#include "tofupilot/models/run_get_validators.hpp"
#include "tofupilot/models/validators_outcome.hpp"

namespace tofupilot {

struct RunGetMeasurements {
    /// Measurement ID.
    std::string id;
    /// Measurement name.
    std::string name;
    /// Measurement validation result.
    ValidatorsOutcome outcome;
    /// Units of measurement.
    std::optional<std::string> units;
    /// Structured validation rules with outcome and expected values.
    std::optional<std::vector<RunGetValidators>> validators;
    /// Aggregations computed over this measurement.
    NullableField<std::vector<RunGetAggregations>> aggregations;
    /// The actual measured value.
    std::optional<nlohmann::json> measured_value;
    /// Multi-dimensional measurement data series.
    std::optional<std::vector<RunGetDataSeries>> data_series;
};

inline void to_json(nlohmann::json& j, const RunGetMeasurements& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    j["name"] = v.name;
    j["outcome"] = v.outcome;
    if (v.units.has_value()) {
        j["units"] = v.units.value();
    }
    if (v.validators.has_value()) {
        j["validators"] = v.validators.value();
    }
    if (!v.aggregations.is_absent()) {
        if (v.aggregations.is_null()) {
            j["aggregations"] = nullptr;
        } else {
            j["aggregations"] = v.aggregations.get();
        }
    }
    if (v.measured_value.has_value()) {
        j["measured_value"] = v.measured_value.value();
    }
    if (v.data_series.has_value()) {
        j["data_series"] = v.data_series.value();
    }
}

inline void from_json(const nlohmann::json& j, RunGetMeasurements& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (!j.contains("name")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: name", &j);
    }
    v.name = j["name"].get<std::string>();
    if (!j.contains("outcome")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: outcome", &j);
    }
    v.outcome = j["outcome"].get<ValidatorsOutcome>();
    if (j.contains("units") && !j["units"].is_null()) {
        v.units = j["units"].get<std::string>();
    }
    if (j.contains("validators") && !j["validators"].is_null()) {
        v.validators = j["validators"].get<std::vector<RunGetValidators>>();
    }
    if (j.contains("aggregations")) {
        if (j["aggregations"].is_null()) {
            v.aggregations = NullableField<std::vector<RunGetAggregations>>::make_null();
        } else {
            v.aggregations = NullableField<std::vector<RunGetAggregations>>::value(j["aggregations"].get<std::vector<RunGetAggregations>>());
        }
    }
    if (j.contains("measured_value") && !j["measured_value"].is_null()) {
        v.measured_value = j["measured_value"].get<nlohmann::json>();
    }
    if (j.contains("data_series") && !j["data_series"].is_null()) {
        v.data_series = j["data_series"].get<std::vector<RunGetDataSeries>>();
    }
}

/// Builder for RunGetMeasurements.
class RunGetMeasurementsBuilder {
public:
    /// Set the `id` field.
    /// Measurement ID.
    RunGetMeasurementsBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    /// Set the `name` field.
    /// Measurement name.
    RunGetMeasurementsBuilder& name(std::string value) {
        name_ = std::move(value);
        return *this;
    }

    /// Set the `outcome` field.
    /// Measurement validation result.
    RunGetMeasurementsBuilder& outcome(ValidatorsOutcome value) {
        outcome_ = std::move(value);
        return *this;
    }

    /// Set the `units` field.
    /// Units of measurement.
    RunGetMeasurementsBuilder& units(std::string value) {
        units_ = std::move(value);
        return *this;
    }

    /// Set the `validators` field.
    /// Structured validation rules with outcome and expected values.
    RunGetMeasurementsBuilder& validators(std::vector<RunGetValidators> value) {
        validators_ = std::move(value);
        return *this;
    }

    /// Set the `aggregations` field.
    /// Aggregations computed over this measurement.
    RunGetMeasurementsBuilder& aggregations(std::vector<RunGetAggregations> value) {
        aggregations_ = NullableField<std::vector<RunGetAggregations>>::value(std::move(value));
        return *this;
    }

    /// Explicitly set `aggregations` to null.
    RunGetMeasurementsBuilder& aggregations_null() {
        aggregations_ = NullableField<std::vector<RunGetAggregations>>::make_null();
        return *this;
    }

    /// Set the `measured_value` field.
    /// The actual measured value.
    RunGetMeasurementsBuilder& measured_value(nlohmann::json value) {
        measured_value_ = std::move(value);
        return *this;
    }

    /// Set the `data_series` field.
    /// Multi-dimensional measurement data series.
    RunGetMeasurementsBuilder& data_series(std::vector<RunGetDataSeries> value) {
        data_series_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    RunGetMeasurements build() const& {
        RunGetMeasurements result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = id_.value();
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = name_.value();
        if (!outcome_.has_value()) {
            throw std::runtime_error("missing required field: outcome");
        }
        result.outcome = outcome_.value();
        result.units = units_;
        result.validators = validators_;
        result.aggregations = aggregations_;
        result.measured_value = measured_value_;
        result.data_series = data_series_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    RunGetMeasurements build() && {
        RunGetMeasurements result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = std::move(id_.value());
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = std::move(name_.value());
        if (!outcome_.has_value()) {
            throw std::runtime_error("missing required field: outcome");
        }
        result.outcome = std::move(outcome_.value());
        result.units = std::move(units_);
        result.validators = std::move(validators_);
        result.aggregations = std::move(aggregations_);
        result.measured_value = std::move(measured_value_);
        result.data_series = std::move(data_series_);
        return result;
    }

private:
    std::optional<std::string> id_;
    std::optional<std::string> name_;
    std::optional<ValidatorsOutcome> outcome_;
    std::optional<std::string> units_;
    std::optional<std::vector<RunGetValidators>> validators_;
    NullableField<std::vector<RunGetAggregations>> aggregations_;
    std::optional<nlohmann::json> measured_value_;
    std::optional<std::vector<RunGetDataSeries>> data_series_;
};

} // namespace tofupilot
