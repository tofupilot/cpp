// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>


namespace tofupilot {

struct ImportCreateFromFilesResults {
    /// Upload ID this result corresponds to.
    std::string upload_id;
    /// Whether the file was imported successfully.
    bool success;
    /// ID of the created run (present when success is true). For a multi-run file this is the first run; see `ids`.
    std::optional<std::string> id;
    /// All run ids created from the file. Present when the file produced more than one run.
    std::optional<std::vector<std::string>> ids;
    /// Error message (present when success is false).
    std::optional<std::string> error;
};

inline void to_json(nlohmann::json& j, const ImportCreateFromFilesResults& v) {
    j = nlohmann::json::object();
    j["upload_id"] = v.upload_id;
    j["success"] = v.success;
    if (v.id.has_value()) {
        j["id"] = v.id.value();
    }
    if (v.ids.has_value()) {
        j["ids"] = v.ids.value();
    }
    if (v.error.has_value()) {
        j["error"] = v.error.value();
    }
}

inline void from_json(const nlohmann::json& j, ImportCreateFromFilesResults& v) {
    if (!j.contains("upload_id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: upload_id", &j);
    }
    v.upload_id = j["upload_id"].get<std::string>();
    if (!j.contains("success")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: success", &j);
    }
    v.success = j["success"].get<bool>();
    if (j.contains("id") && !j["id"].is_null()) {
        v.id = j["id"].get<std::string>();
    }
    if (j.contains("ids") && !j["ids"].is_null()) {
        v.ids = j["ids"].get<std::vector<std::string>>();
    }
    if (j.contains("error") && !j["error"].is_null()) {
        v.error = j["error"].get<std::string>();
    }
}

/// Builder for ImportCreateFromFilesResults.
class ImportCreateFromFilesResultsBuilder {
public:
    /// Set the `upload_id` field.
    /// Upload ID this result corresponds to.
    ImportCreateFromFilesResultsBuilder& upload_id(std::string value) {
        upload_id_ = std::move(value);
        return *this;
    }

    /// Set the `success` field.
    /// Whether the file was imported successfully.
    ImportCreateFromFilesResultsBuilder& success(bool value) {
        success_ = std::move(value);
        return *this;
    }

    /// Set the `id` field.
    /// ID of the created run (present when success is true). For a multi-run file this is the first run; see `ids`.
    ImportCreateFromFilesResultsBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    /// Set the `ids` field.
    /// All run ids created from the file. Present when the file produced more than one run.
    ImportCreateFromFilesResultsBuilder& ids(std::vector<std::string> value) {
        ids_ = std::move(value);
        return *this;
    }

    /// Set the `error` field.
    /// Error message (present when success is false).
    ImportCreateFromFilesResultsBuilder& error(std::string value) {
        error_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    ImportCreateFromFilesResults build() const& {
        ImportCreateFromFilesResults result;
        if (!upload_id_.has_value()) {
            throw std::runtime_error("missing required field: upload_id");
        }
        result.upload_id = upload_id_.value();
        if (!success_.has_value()) {
            throw std::runtime_error("missing required field: success");
        }
        result.success = success_.value();
        result.id = id_;
        result.ids = ids_;
        result.error = error_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    ImportCreateFromFilesResults build() && {
        ImportCreateFromFilesResults result;
        if (!upload_id_.has_value()) {
            throw std::runtime_error("missing required field: upload_id");
        }
        result.upload_id = std::move(upload_id_.value());
        if (!success_.has_value()) {
            throw std::runtime_error("missing required field: success");
        }
        result.success = std::move(success_.value());
        result.id = std::move(id_);
        result.ids = std::move(ids_);
        result.error = std::move(error_);
        return result;
    }

private:
    std::optional<std::string> upload_id_;
    std::optional<bool> success_;
    std::optional<std::string> id_;
    std::optional<std::vector<std::string>> ids_;
    std::optional<std::string> error_;
};

} // namespace tofupilot
