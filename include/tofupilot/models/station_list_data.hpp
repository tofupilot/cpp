// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/station_list_procedures.hpp"
#include "tofupilot/models/station_list_team.hpp"

namespace tofupilot {

struct StationListData {
    /// Unique identifier of the station
    std::string id;
    /// Name of the station
    std::string name;
    /// Procedures linked to this station
    std::vector<StationListProcedures> procedures;
    /// Total number of procedures linked to this station
    double procedures_count;
    /// Team this station belongs to
    std::optional<StationListTeam> team;
};

inline void to_json(nlohmann::json& j, const StationListData& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    j["name"] = v.name;
    j["procedures"] = v.procedures;
    j["procedures_count"] = v.procedures_count;
    if (v.team.has_value()) {
        j["team"] = v.team.value();
    }
}

inline void from_json(const nlohmann::json& j, StationListData& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (!j.contains("name")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: name", &j);
    }
    v.name = j["name"].get<std::string>();
    if (j.contains("procedures") && j["procedures"].is_array()) {
        v.procedures = j["procedures"].get<std::vector<StationListProcedures>>();
    }
    if (!j.contains("procedures_count")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: procedures_count", &j);
    }
    v.procedures_count = j["procedures_count"].get<double>();
    if (j.contains("team") && !j["team"].is_null()) {
        v.team = j["team"].get<StationListTeam>();
    }
}

/// Builder for StationListData.
class StationListDataBuilder {
public:
    /// Set the `id` field.
    /// Unique identifier of the station
    StationListDataBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    /// Set the `name` field.
    /// Name of the station
    StationListDataBuilder& name(std::string value) {
        name_ = std::move(value);
        return *this;
    }

    /// Set the `procedures` field.
    /// Procedures linked to this station
    StationListDataBuilder& procedures(std::vector<StationListProcedures> value) {
        procedures_ = std::move(value);
        return *this;
    }

    /// Set the `procedures_count` field.
    /// Total number of procedures linked to this station
    StationListDataBuilder& procedures_count(double value) {
        procedures_count_ = std::move(value);
        return *this;
    }

    /// Set the `team` field.
    /// Team this station belongs to
    StationListDataBuilder& team(StationListTeam value) {
        team_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    StationListData build() const& {
        StationListData result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = id_.value();
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = name_.value();
        if (procedures_.has_value()) {
            result.procedures = procedures_.value();
        }
        if (!procedures_count_.has_value()) {
            throw std::runtime_error("missing required field: procedures_count");
        }
        result.procedures_count = procedures_count_.value();
        result.team = team_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    StationListData build() && {
        StationListData result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = std::move(id_.value());
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = std::move(name_.value());
        if (procedures_.has_value()) {
            result.procedures = std::move(procedures_.value());
        }
        if (!procedures_count_.has_value()) {
            throw std::runtime_error("missing required field: procedures_count");
        }
        result.procedures_count = std::move(procedures_count_.value());
        result.team = std::move(team_);
        return result;
    }

private:
    std::optional<std::string> id_;
    std::optional<std::string> name_;
    std::optional<std::vector<StationListProcedures>> procedures_;
    std::optional<double> procedures_count_;
    std::optional<StationListTeam> team_;
};

} // namespace tofupilot
