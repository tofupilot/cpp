// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/nullable.hpp"
#include "tofupilot/models/outcome.hpp"
#include "tofupilot/models/run_list_created_by_station.hpp"
#include "tofupilot/models/run_list_created_by_user.hpp"
#include "tofupilot/models/run_list_operated_by.hpp"
#include "tofupilot/models/run_list_procedure.hpp"
#include "tofupilot/models/run_list_unit.hpp"

namespace tofupilot {

struct RunListData {
    /// Unique identifier for the run.
    std::string id;
    /// ISO 8601 timestamp when the run was created.
    std::string created_at;
    /// ISO 8601 timestamp when the run execution started.
    std::string started_at;
    /// ISO 8601 timestamp when the run execution ended.
    std::string ended_at;
    /// ISO 8601 duration of the run (computed from started_at and ended_at).
    std::string duration;
    /// Final result of the run execution.
    Outcome outcome;
    /// Additional notes or documentation about this test run.
    NullableField<std::string> docstring;
    /// User whose API key was used to create this run. Only returned if `all` or `created_by` is included.
    NullableField<RunListCreatedByUser> created_by_user;
    /// Station whose API key was used to create this run. Only returned if `all` or `created_by` is included.
    NullableField<RunListCreatedByStation> created_by_station;
    /// User who operated this run. Only returned if `all` or `operated_by` is included.
    NullableField<RunListOperatedBy> operated_by;
    /// Test procedure associated with this run.
    RunListProcedure procedure;
    /// Unit under test information.
    RunListUnit unit;
};

inline void to_json(nlohmann::json& j, const RunListData& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    j["created_at"] = v.created_at;
    j["started_at"] = v.started_at;
    j["ended_at"] = v.ended_at;
    j["duration"] = v.duration;
    j["outcome"] = v.outcome;
    if (!v.docstring.is_absent()) {
        if (v.docstring.is_null()) {
            j["docstring"] = nullptr;
        } else {
            j["docstring"] = v.docstring.get();
        }
    }
    if (!v.created_by_user.is_absent()) {
        if (v.created_by_user.is_null()) {
            j["created_by_user"] = nullptr;
        } else {
            j["created_by_user"] = v.created_by_user.get();
        }
    }
    if (!v.created_by_station.is_absent()) {
        if (v.created_by_station.is_null()) {
            j["created_by_station"] = nullptr;
        } else {
            j["created_by_station"] = v.created_by_station.get();
        }
    }
    if (!v.operated_by.is_absent()) {
        if (v.operated_by.is_null()) {
            j["operated_by"] = nullptr;
        } else {
            j["operated_by"] = v.operated_by.get();
        }
    }
    j["procedure"] = v.procedure;
    j["unit"] = v.unit;
}

inline void from_json(const nlohmann::json& j, RunListData& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (!j.contains("created_at")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: created_at", &j);
    }
    v.created_at = j["created_at"].get<std::string>();
    if (!j.contains("started_at")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: started_at", &j);
    }
    v.started_at = j["started_at"].get<std::string>();
    if (!j.contains("ended_at")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: ended_at", &j);
    }
    v.ended_at = j["ended_at"].get<std::string>();
    if (!j.contains("duration")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: duration", &j);
    }
    v.duration = j["duration"].get<std::string>();
    if (!j.contains("outcome")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: outcome", &j);
    }
    v.outcome = j["outcome"].get<Outcome>();
    if (j.contains("docstring")) {
        if (j["docstring"].is_null()) {
            v.docstring = NullableField<std::string>::make_null();
        } else {
            v.docstring = NullableField<std::string>::value(j["docstring"].get<std::string>());
        }
    }
    if (j.contains("created_by_user")) {
        if (j["created_by_user"].is_null()) {
            v.created_by_user = NullableField<RunListCreatedByUser>::make_null();
        } else {
            v.created_by_user = NullableField<RunListCreatedByUser>::value(j["created_by_user"].get<RunListCreatedByUser>());
        }
    }
    if (j.contains("created_by_station")) {
        if (j["created_by_station"].is_null()) {
            v.created_by_station = NullableField<RunListCreatedByStation>::make_null();
        } else {
            v.created_by_station = NullableField<RunListCreatedByStation>::value(j["created_by_station"].get<RunListCreatedByStation>());
        }
    }
    if (j.contains("operated_by")) {
        if (j["operated_by"].is_null()) {
            v.operated_by = NullableField<RunListOperatedBy>::make_null();
        } else {
            v.operated_by = NullableField<RunListOperatedBy>::value(j["operated_by"].get<RunListOperatedBy>());
        }
    }
    if (!j.contains("procedure")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: procedure", &j);
    }
    v.procedure = j["procedure"].get<RunListProcedure>();
    if (!j.contains("unit")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: unit", &j);
    }
    v.unit = j["unit"].get<RunListUnit>();
}

/// Builder for RunListData.
class RunListDataBuilder {
public:
    /// Set the `id` field.
    /// Unique identifier for the run.
    RunListDataBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    /// Set the `created_at` field.
    /// ISO 8601 timestamp when the run was created.
    RunListDataBuilder& created_at(std::string value) {
        created_at_ = std::move(value);
        return *this;
    }

    /// Set the `started_at` field.
    /// ISO 8601 timestamp when the run execution started.
    RunListDataBuilder& started_at(std::string value) {
        started_at_ = std::move(value);
        return *this;
    }

    /// Set the `ended_at` field.
    /// ISO 8601 timestamp when the run execution ended.
    RunListDataBuilder& ended_at(std::string value) {
        ended_at_ = std::move(value);
        return *this;
    }

    /// Set the `duration` field.
    /// ISO 8601 duration of the run (computed from started_at and ended_at).
    RunListDataBuilder& duration(std::string value) {
        duration_ = std::move(value);
        return *this;
    }

    /// Set the `outcome` field.
    /// Final result of the run execution.
    RunListDataBuilder& outcome(Outcome value) {
        outcome_ = std::move(value);
        return *this;
    }

    /// Set the `docstring` field.
    /// Additional notes or documentation about this test run.
    RunListDataBuilder& docstring(std::string value) {
        docstring_ = NullableField<std::string>::value(std::move(value));
        return *this;
    }

    /// Explicitly set `docstring` to null.
    RunListDataBuilder& docstring_null() {
        docstring_ = NullableField<std::string>::make_null();
        return *this;
    }

    /// Set the `created_by_user` field.
    /// User whose API key was used to create this run. Only returned if `all` or `created_by` is included.
    RunListDataBuilder& created_by_user(RunListCreatedByUser value) {
        created_by_user_ = NullableField<RunListCreatedByUser>::value(std::move(value));
        return *this;
    }

    /// Explicitly set `created_by_user` to null.
    RunListDataBuilder& created_by_user_null() {
        created_by_user_ = NullableField<RunListCreatedByUser>::make_null();
        return *this;
    }

    /// Set the `created_by_station` field.
    /// Station whose API key was used to create this run. Only returned if `all` or `created_by` is included.
    RunListDataBuilder& created_by_station(RunListCreatedByStation value) {
        created_by_station_ = NullableField<RunListCreatedByStation>::value(std::move(value));
        return *this;
    }

    /// Explicitly set `created_by_station` to null.
    RunListDataBuilder& created_by_station_null() {
        created_by_station_ = NullableField<RunListCreatedByStation>::make_null();
        return *this;
    }

    /// Set the `operated_by` field.
    /// User who operated this run. Only returned if `all` or `operated_by` is included.
    RunListDataBuilder& operated_by(RunListOperatedBy value) {
        operated_by_ = NullableField<RunListOperatedBy>::value(std::move(value));
        return *this;
    }

    /// Explicitly set `operated_by` to null.
    RunListDataBuilder& operated_by_null() {
        operated_by_ = NullableField<RunListOperatedBy>::make_null();
        return *this;
    }

    /// Set the `procedure` field.
    /// Test procedure associated with this run.
    RunListDataBuilder& procedure(RunListProcedure value) {
        procedure_ = std::move(value);
        return *this;
    }

    /// Set the `unit` field.
    /// Unit under test information.
    RunListDataBuilder& unit(RunListUnit value) {
        unit_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    RunListData build() const& {
        RunListData result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = id_.value();
        if (!created_at_.has_value()) {
            throw std::runtime_error("missing required field: created_at");
        }
        result.created_at = created_at_.value();
        if (!started_at_.has_value()) {
            throw std::runtime_error("missing required field: started_at");
        }
        result.started_at = started_at_.value();
        if (!ended_at_.has_value()) {
            throw std::runtime_error("missing required field: ended_at");
        }
        result.ended_at = ended_at_.value();
        if (!duration_.has_value()) {
            throw std::runtime_error("missing required field: duration");
        }
        result.duration = duration_.value();
        if (!outcome_.has_value()) {
            throw std::runtime_error("missing required field: outcome");
        }
        result.outcome = outcome_.value();
        result.docstring = docstring_;
        result.created_by_user = created_by_user_;
        result.created_by_station = created_by_station_;
        result.operated_by = operated_by_;
        if (!procedure_.has_value()) {
            throw std::runtime_error("missing required field: procedure");
        }
        result.procedure = procedure_.value();
        if (!unit_.has_value()) {
            throw std::runtime_error("missing required field: unit");
        }
        result.unit = unit_.value();
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    RunListData build() && {
        RunListData result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = std::move(id_.value());
        if (!created_at_.has_value()) {
            throw std::runtime_error("missing required field: created_at");
        }
        result.created_at = std::move(created_at_.value());
        if (!started_at_.has_value()) {
            throw std::runtime_error("missing required field: started_at");
        }
        result.started_at = std::move(started_at_.value());
        if (!ended_at_.has_value()) {
            throw std::runtime_error("missing required field: ended_at");
        }
        result.ended_at = std::move(ended_at_.value());
        if (!duration_.has_value()) {
            throw std::runtime_error("missing required field: duration");
        }
        result.duration = std::move(duration_.value());
        if (!outcome_.has_value()) {
            throw std::runtime_error("missing required field: outcome");
        }
        result.outcome = std::move(outcome_.value());
        result.docstring = std::move(docstring_);
        result.created_by_user = std::move(created_by_user_);
        result.created_by_station = std::move(created_by_station_);
        result.operated_by = std::move(operated_by_);
        if (!procedure_.has_value()) {
            throw std::runtime_error("missing required field: procedure");
        }
        result.procedure = std::move(procedure_.value());
        if (!unit_.has_value()) {
            throw std::runtime_error("missing required field: unit");
        }
        result.unit = std::move(unit_.value());
        return result;
    }

private:
    std::optional<std::string> id_;
    std::optional<std::string> created_at_;
    std::optional<std::string> started_at_;
    std::optional<std::string> ended_at_;
    std::optional<std::string> duration_;
    std::optional<Outcome> outcome_;
    NullableField<std::string> docstring_;
    NullableField<RunListCreatedByUser> created_by_user_;
    NullableField<RunListCreatedByStation> created_by_station_;
    NullableField<RunListOperatedBy> operated_by_;
    std::optional<RunListProcedure> procedure_;
    std::optional<RunListUnit> unit_;
};

} // namespace tofupilot
