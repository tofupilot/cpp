// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/outcome.hpp"
#include "tofupilot/models/unit_list_procedure.hpp"

namespace tofupilot {

struct UnitListLastRun {
    /// Unique identifier for the run.
    std::string id;
    /// Final result of the test run execution.
    Outcome outcome;
    /// ISO 8601 timestamp when the run execution started.
    std::string started_at;
    /// ISO 8601 timestamp when the run execution completed. Null if still running.
    std::optional<std::string> ended_at;
    /// Test procedure that was executed. Null if run had no associated procedure.
    std::optional<UnitListProcedure> procedure;
};

inline void to_json(nlohmann::json& j, const UnitListLastRun& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    j["outcome"] = v.outcome;
    j["started_at"] = v.started_at;
    if (v.ended_at.has_value()) {
        j["ended_at"] = v.ended_at.value();
    }
    if (v.procedure.has_value()) {
        j["procedure"] = v.procedure.value();
    }
}

inline void from_json(const nlohmann::json& j, UnitListLastRun& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (!j.contains("outcome")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: outcome", &j);
    }
    v.outcome = j["outcome"].get<Outcome>();
    if (!j.contains("started_at")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: started_at", &j);
    }
    v.started_at = j["started_at"].get<std::string>();
    if (j.contains("ended_at") && !j["ended_at"].is_null()) {
        v.ended_at = j["ended_at"].get<std::string>();
    }
    if (j.contains("procedure") && !j["procedure"].is_null()) {
        v.procedure = j["procedure"].get<UnitListProcedure>();
    }
}

/// Builder for UnitListLastRun.
class UnitListLastRunBuilder {
public:
    /// Set the `id` field.
    /// Unique identifier for the run.
    UnitListLastRunBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    /// Set the `outcome` field.
    /// Final result of the test run execution.
    UnitListLastRunBuilder& outcome(Outcome value) {
        outcome_ = std::move(value);
        return *this;
    }

    /// Set the `started_at` field.
    /// ISO 8601 timestamp when the run execution started.
    UnitListLastRunBuilder& started_at(std::string value) {
        started_at_ = std::move(value);
        return *this;
    }

    /// Set the `ended_at` field.
    /// ISO 8601 timestamp when the run execution completed. Null if still running.
    UnitListLastRunBuilder& ended_at(std::string value) {
        ended_at_ = std::move(value);
        return *this;
    }

    /// Set the `procedure` field.
    /// Test procedure that was executed. Null if run had no associated procedure.
    UnitListLastRunBuilder& procedure(UnitListProcedure value) {
        procedure_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    UnitListLastRun build() const& {
        UnitListLastRun result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = id_.value();
        if (!outcome_.has_value()) {
            throw std::runtime_error("missing required field: outcome");
        }
        result.outcome = outcome_.value();
        if (!started_at_.has_value()) {
            throw std::runtime_error("missing required field: started_at");
        }
        result.started_at = started_at_.value();
        result.ended_at = ended_at_;
        result.procedure = procedure_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    UnitListLastRun build() && {
        UnitListLastRun result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = std::move(id_.value());
        if (!outcome_.has_value()) {
            throw std::runtime_error("missing required field: outcome");
        }
        result.outcome = std::move(outcome_.value());
        if (!started_at_.has_value()) {
            throw std::runtime_error("missing required field: started_at");
        }
        result.started_at = std::move(started_at_.value());
        result.ended_at = std::move(ended_at_);
        result.procedure = std::move(procedure_);
        return result;
    }

private:
    std::optional<std::string> id_;
    std::optional<Outcome> outcome_;
    std::optional<std::string> started_at_;
    std::optional<std::string> ended_at_;
    std::optional<UnitListProcedure> procedure_;
};

} // namespace tofupilot
