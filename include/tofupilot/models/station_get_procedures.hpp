// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/nullable.hpp"
#include "tofupilot/models/station_get_deployment.hpp"

namespace tofupilot {

struct StationGetProcedures {
    /// Procedure ID
    std::string id;
    /// Procedure identifier
    std::optional<std::string> identifier;
    /// Procedure name
    std::string name;
    /// Number of runs created by this station in the last 7 days
    double runs_count;
    /// Deployment information for this procedure on this station
    NullableField<StationGetDeployment> deployment;
};

inline void to_json(nlohmann::json& j, const StationGetProcedures& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    if (v.identifier.has_value()) {
        j["identifier"] = v.identifier.value();
    }
    j["name"] = v.name;
    j["runs_count"] = v.runs_count;
    if (!v.deployment.is_absent()) {
        if (v.deployment.is_null()) {
            j["deployment"] = nullptr;
        } else {
            j["deployment"] = v.deployment.get();
        }
    }
}

inline void from_json(const nlohmann::json& j, StationGetProcedures& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (j.contains("identifier") && !j["identifier"].is_null()) {
        v.identifier = j["identifier"].get<std::string>();
    }
    if (!j.contains("name")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: name", &j);
    }
    v.name = j["name"].get<std::string>();
    if (!j.contains("runs_count")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: runs_count", &j);
    }
    v.runs_count = j["runs_count"].get<double>();
    if (j.contains("deployment")) {
        if (j["deployment"].is_null()) {
            v.deployment = NullableField<StationGetDeployment>::make_null();
        } else {
            v.deployment = NullableField<StationGetDeployment>::value(j["deployment"].get<StationGetDeployment>());
        }
    }
}

/// Builder for StationGetProcedures.
class StationGetProceduresBuilder {
public:
    /// Set the `id` field.
    /// Procedure ID
    StationGetProceduresBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    /// Set the `identifier` field.
    /// Procedure identifier
    StationGetProceduresBuilder& identifier(std::string value) {
        identifier_ = std::move(value);
        return *this;
    }

    /// Set the `name` field.
    /// Procedure name
    StationGetProceduresBuilder& name(std::string value) {
        name_ = std::move(value);
        return *this;
    }

    /// Set the `runs_count` field.
    /// Number of runs created by this station in the last 7 days
    StationGetProceduresBuilder& runs_count(double value) {
        runs_count_ = std::move(value);
        return *this;
    }

    /// Set the `deployment` field.
    /// Deployment information for this procedure on this station
    StationGetProceduresBuilder& deployment(StationGetDeployment value) {
        deployment_ = NullableField<StationGetDeployment>::value(std::move(value));
        return *this;
    }

    /// Explicitly set `deployment` to null.
    StationGetProceduresBuilder& deployment_null() {
        deployment_ = NullableField<StationGetDeployment>::make_null();
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    StationGetProcedures build() const& {
        StationGetProcedures result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = id_.value();
        result.identifier = identifier_;
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = name_.value();
        if (!runs_count_.has_value()) {
            throw std::runtime_error("missing required field: runs_count");
        }
        result.runs_count = runs_count_.value();
        result.deployment = deployment_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    StationGetProcedures build() && {
        StationGetProcedures result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = std::move(id_.value());
        result.identifier = std::move(identifier_);
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = std::move(name_.value());
        if (!runs_count_.has_value()) {
            throw std::runtime_error("missing required field: runs_count");
        }
        result.runs_count = std::move(runs_count_.value());
        result.deployment = std::move(deployment_);
        return result;
    }

private:
    std::optional<std::string> id_;
    std::optional<std::string> identifier_;
    std::optional<std::string> name_;
    std::optional<double> runs_count_;
    NullableField<StationGetDeployment> deployment_;
};

} // namespace tofupilot
