// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/nullable.hpp"
#include "tofupilot/models/level.hpp"
#include "tofupilot/models/log_list_created_by_station.hpp"
#include "tofupilot/models/log_list_created_by_user.hpp"
#include "tofupilot/models/log_list_procedure.hpp"
#include "tofupilot/models/log_list_run.hpp"
#include "tofupilot/models/log_list_unit.hpp"

namespace tofupilot {

struct LogListData {
    /// Unique identifier for the log entry.
    std::string id;
    /// Log level indicating the severity.
    Level level;
    /// The log message content.
    std::string message;
    /// ISO 8601 timestamp when the log was created.
    std::string created_at;
    /// Source file where the log originated.
    std::optional<std::string> source_file;
    /// Line number in the source file.
    std::optional<int64_t> line_number;
    /// Run information for this log entry.
    LogListRun run;
    /// Unit information for this log entry.
    LogListUnit unit;
    /// Procedure information for this log entry.
    LogListProcedure procedure;
    /// User who created this log. Null if created by a station or system.
    NullableField<LogListCreatedByUser> created_by_user;
    /// Station that created this log. Null if created by a user.
    NullableField<LogListCreatedByStation> created_by_station;
};

inline void to_json(nlohmann::json& j, const LogListData& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    j["level"] = v.level;
    j["message"] = v.message;
    j["created_at"] = v.created_at;
    if (v.source_file.has_value()) {
        j["source_file"] = v.source_file.value();
    }
    if (v.line_number.has_value()) {
        j["line_number"] = v.line_number.value();
    }
    j["run"] = v.run;
    j["unit"] = v.unit;
    j["procedure"] = v.procedure;
    if (!v.created_by_user.is_absent()) {
        if (v.created_by_user.is_null()) {
            j["created_by_user"] = nullptr;
        } else {
            j["created_by_user"] = v.created_by_user.get();
        }
    }
    if (!v.created_by_station.is_absent()) {
        if (v.created_by_station.is_null()) {
            j["created_by_station"] = nullptr;
        } else {
            j["created_by_station"] = v.created_by_station.get();
        }
    }
}

inline void from_json(const nlohmann::json& j, LogListData& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (!j.contains("level")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: level", &j);
    }
    v.level = j["level"].get<Level>();
    if (!j.contains("message")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: message", &j);
    }
    v.message = j["message"].get<std::string>();
    if (!j.contains("created_at")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: created_at", &j);
    }
    v.created_at = j["created_at"].get<std::string>();
    if (j.contains("source_file") && !j["source_file"].is_null()) {
        v.source_file = j["source_file"].get<std::string>();
    }
    if (j.contains("line_number") && !j["line_number"].is_null()) {
        v.line_number = j["line_number"].get<int64_t>();
    }
    if (!j.contains("run")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: run", &j);
    }
    v.run = j["run"].get<LogListRun>();
    if (!j.contains("unit")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: unit", &j);
    }
    v.unit = j["unit"].get<LogListUnit>();
    if (!j.contains("procedure")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: procedure", &j);
    }
    v.procedure = j["procedure"].get<LogListProcedure>();
    if (j.contains("created_by_user")) {
        if (j["created_by_user"].is_null()) {
            v.created_by_user = NullableField<LogListCreatedByUser>::make_null();
        } else {
            v.created_by_user = NullableField<LogListCreatedByUser>::value(j["created_by_user"].get<LogListCreatedByUser>());
        }
    }
    if (j.contains("created_by_station")) {
        if (j["created_by_station"].is_null()) {
            v.created_by_station = NullableField<LogListCreatedByStation>::make_null();
        } else {
            v.created_by_station = NullableField<LogListCreatedByStation>::value(j["created_by_station"].get<LogListCreatedByStation>());
        }
    }
}

/// Builder for LogListData.
class LogListDataBuilder {
public:
    /// Set the `id` field.
    /// Unique identifier for the log entry.
    LogListDataBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    /// Set the `level` field.
    /// Log level indicating the severity.
    LogListDataBuilder& level(Level value) {
        level_ = std::move(value);
        return *this;
    }

    /// Set the `message` field.
    /// The log message content.
    LogListDataBuilder& message(std::string value) {
        message_ = std::move(value);
        return *this;
    }

    /// Set the `created_at` field.
    /// ISO 8601 timestamp when the log was created.
    LogListDataBuilder& created_at(std::string value) {
        created_at_ = std::move(value);
        return *this;
    }

    /// Set the `source_file` field.
    /// Source file where the log originated.
    LogListDataBuilder& source_file(std::string value) {
        source_file_ = std::move(value);
        return *this;
    }

    /// Set the `line_number` field.
    /// Line number in the source file.
    LogListDataBuilder& line_number(int64_t value) {
        line_number_ = std::move(value);
        return *this;
    }

    /// Set the `run` field.
    /// Run information for this log entry.
    LogListDataBuilder& run(LogListRun value) {
        run_ = std::move(value);
        return *this;
    }

    /// Set the `unit` field.
    /// Unit information for this log entry.
    LogListDataBuilder& unit(LogListUnit value) {
        unit_ = std::move(value);
        return *this;
    }

    /// Set the `procedure` field.
    /// Procedure information for this log entry.
    LogListDataBuilder& procedure(LogListProcedure value) {
        procedure_ = std::move(value);
        return *this;
    }

    /// Set the `created_by_user` field.
    /// User who created this log. Null if created by a station or system.
    LogListDataBuilder& created_by_user(LogListCreatedByUser value) {
        created_by_user_ = NullableField<LogListCreatedByUser>::value(std::move(value));
        return *this;
    }

    /// Explicitly set `created_by_user` to null.
    LogListDataBuilder& created_by_user_null() {
        created_by_user_ = NullableField<LogListCreatedByUser>::make_null();
        return *this;
    }

    /// Set the `created_by_station` field.
    /// Station that created this log. Null if created by a user.
    LogListDataBuilder& created_by_station(LogListCreatedByStation value) {
        created_by_station_ = NullableField<LogListCreatedByStation>::value(std::move(value));
        return *this;
    }

    /// Explicitly set `created_by_station` to null.
    LogListDataBuilder& created_by_station_null() {
        created_by_station_ = NullableField<LogListCreatedByStation>::make_null();
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    LogListData build() const& {
        LogListData result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = id_.value();
        if (!level_.has_value()) {
            throw std::runtime_error("missing required field: level");
        }
        result.level = level_.value();
        if (!message_.has_value()) {
            throw std::runtime_error("missing required field: message");
        }
        result.message = message_.value();
        if (!created_at_.has_value()) {
            throw std::runtime_error("missing required field: created_at");
        }
        result.created_at = created_at_.value();
        result.source_file = source_file_;
        result.line_number = line_number_;
        if (!run_.has_value()) {
            throw std::runtime_error("missing required field: run");
        }
        result.run = run_.value();
        if (!unit_.has_value()) {
            throw std::runtime_error("missing required field: unit");
        }
        result.unit = unit_.value();
        if (!procedure_.has_value()) {
            throw std::runtime_error("missing required field: procedure");
        }
        result.procedure = procedure_.value();
        result.created_by_user = created_by_user_;
        result.created_by_station = created_by_station_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    LogListData build() && {
        LogListData result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = std::move(id_.value());
        if (!level_.has_value()) {
            throw std::runtime_error("missing required field: level");
        }
        result.level = std::move(level_.value());
        if (!message_.has_value()) {
            throw std::runtime_error("missing required field: message");
        }
        result.message = std::move(message_.value());
        if (!created_at_.has_value()) {
            throw std::runtime_error("missing required field: created_at");
        }
        result.created_at = std::move(created_at_.value());
        result.source_file = std::move(source_file_);
        result.line_number = std::move(line_number_);
        if (!run_.has_value()) {
            throw std::runtime_error("missing required field: run");
        }
        result.run = std::move(run_.value());
        if (!unit_.has_value()) {
            throw std::runtime_error("missing required field: unit");
        }
        result.unit = std::move(unit_.value());
        if (!procedure_.has_value()) {
            throw std::runtime_error("missing required field: procedure");
        }
        result.procedure = std::move(procedure_.value());
        result.created_by_user = std::move(created_by_user_);
        result.created_by_station = std::move(created_by_station_);
        return result;
    }

private:
    std::optional<std::string> id_;
    std::optional<Level> level_;
    std::optional<std::string> message_;
    std::optional<std::string> created_at_;
    std::optional<std::string> source_file_;
    std::optional<int64_t> line_number_;
    std::optional<LogListRun> run_;
    std::optional<LogListUnit> unit_;
    std::optional<LogListProcedure> procedure_;
    NullableField<LogListCreatedByUser> created_by_user_;
    NullableField<LogListCreatedByStation> created_by_station_;
};

} // namespace tofupilot
