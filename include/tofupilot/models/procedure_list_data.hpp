// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/procedure_list_created_by_user.hpp"
#include "tofupilot/models/procedure_list_linked_repository.hpp"

namespace tofupilot {

struct ProcedureListData {
    /// Unique identifier for the procedure.
    std::string id;
    /// Name of the procedure.
    std::string name;
    /// ISO 8601 timestamp when the procedure was created.
    std::string created_at;
    /// User who created the procedure.
    ProcedureListCreatedByUser created_by_user;
    /// Linked repository for this procedure.
    std::optional<ProcedureListLinkedRepository> linked_repository;
};

inline void to_json(nlohmann::json& j, const ProcedureListData& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    j["name"] = v.name;
    j["created_at"] = v.created_at;
    j["created_by_user"] = v.created_by_user;
    if (v.linked_repository.has_value()) {
        j["linkedRepository"] = v.linked_repository.value();
    }
}

inline void from_json(const nlohmann::json& j, ProcedureListData& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (!j.contains("name")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: name", &j);
    }
    v.name = j["name"].get<std::string>();
    if (!j.contains("created_at")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: created_at", &j);
    }
    v.created_at = j["created_at"].get<std::string>();
    if (!j.contains("created_by_user")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: created_by_user", &j);
    }
    v.created_by_user = j["created_by_user"].get<ProcedureListCreatedByUser>();
    if (j.contains("linkedRepository") && !j["linkedRepository"].is_null()) {
        v.linked_repository = j["linkedRepository"].get<ProcedureListLinkedRepository>();
    }
}

/// Builder for ProcedureListData.
class ProcedureListDataBuilder {
public:
    /// Set the `id` field.
    /// Unique identifier for the procedure.
    ProcedureListDataBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    /// Set the `name` field.
    /// Name of the procedure.
    ProcedureListDataBuilder& name(std::string value) {
        name_ = std::move(value);
        return *this;
    }

    /// Set the `created_at` field.
    /// ISO 8601 timestamp when the procedure was created.
    ProcedureListDataBuilder& created_at(std::string value) {
        created_at_ = std::move(value);
        return *this;
    }

    /// Set the `created_by_user` field.
    /// User who created the procedure.
    ProcedureListDataBuilder& created_by_user(ProcedureListCreatedByUser value) {
        created_by_user_ = std::move(value);
        return *this;
    }

    /// Set the `linkedRepository` field.
    /// Linked repository for this procedure.
    ProcedureListDataBuilder& linked_repository(ProcedureListLinkedRepository value) {
        linked_repository_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    ProcedureListData build() const& {
        ProcedureListData result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = id_.value();
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = name_.value();
        if (!created_at_.has_value()) {
            throw std::runtime_error("missing required field: created_at");
        }
        result.created_at = created_at_.value();
        if (!created_by_user_.has_value()) {
            throw std::runtime_error("missing required field: created_by_user");
        }
        result.created_by_user = created_by_user_.value();
        result.linked_repository = linked_repository_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    ProcedureListData build() && {
        ProcedureListData result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = std::move(id_.value());
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = std::move(name_.value());
        if (!created_at_.has_value()) {
            throw std::runtime_error("missing required field: created_at");
        }
        result.created_at = std::move(created_at_.value());
        if (!created_by_user_.has_value()) {
            throw std::runtime_error("missing required field: created_by_user");
        }
        result.created_by_user = std::move(created_by_user_.value());
        result.linked_repository = std::move(linked_repository_);
        return result;
    }

private:
    std::optional<std::string> id_;
    std::optional<std::string> name_;
    std::optional<std::string> created_at_;
    std::optional<ProcedureListCreatedByUser> created_by_user_;
    std::optional<ProcedureListLinkedRepository> linked_repository_;
};

} // namespace tofupilot
