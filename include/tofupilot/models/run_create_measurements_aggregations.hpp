// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/nullable.hpp"
#include "tofupilot/models/run_create_measurements_aggregations_validators.hpp"

namespace tofupilot {

/// Aggregation specification with computed value and optional validators.
struct RunCreateMeasurementsAggregations {
    /// Aggregation function: "min", "max", "avg", "sum", "count", "std", "median", "percentile_95", etc.
    std::string type;
    /// Computed result of aggregation validation. Server stores as-is.
    NullableField<std::string> outcome;
    /// Computed aggregation value.
    NullableField<nlohmann::json> value;
    /// Unit for the aggregated value.
    NullableField<std::string> unit;
    /// Validators applied to the aggregated value.
    NullableField<std::vector<RunCreateMeasurementsAggregationsValidators>> validators;
};

inline void to_json(nlohmann::json& j, const RunCreateMeasurementsAggregations& v) {
    j = nlohmann::json::object();
    j["type"] = v.type;
    if (!v.outcome.is_absent()) {
        if (v.outcome.is_null()) {
            j["outcome"] = nullptr;
        } else {
            j["outcome"] = v.outcome.get();
        }
    }
    if (!v.value.is_absent()) {
        if (v.value.is_null()) {
            j["value"] = nullptr;
        } else {
            j["value"] = v.value.get();
        }
    }
    if (!v.unit.is_absent()) {
        if (v.unit.is_null()) {
            j["unit"] = nullptr;
        } else {
            j["unit"] = v.unit.get();
        }
    }
    if (!v.validators.is_absent()) {
        if (v.validators.is_null()) {
            j["validators"] = nullptr;
        } else {
            j["validators"] = v.validators.get();
        }
    }
}

inline void from_json(const nlohmann::json& j, RunCreateMeasurementsAggregations& v) {
    if (!j.contains("type")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: type", &j);
    }
    v.type = j["type"].get<std::string>();
    if (j.contains("outcome")) {
        if (j["outcome"].is_null()) {
            v.outcome = NullableField<std::string>::make_null();
        } else {
            v.outcome = NullableField<std::string>::value(j["outcome"].get<std::string>());
        }
    }
    if (j.contains("value")) {
        if (j["value"].is_null()) {
            v.value = NullableField<nlohmann::json>::make_null();
        } else {
            v.value = NullableField<nlohmann::json>::value(j["value"].get<nlohmann::json>());
        }
    }
    if (j.contains("unit")) {
        if (j["unit"].is_null()) {
            v.unit = NullableField<std::string>::make_null();
        } else {
            v.unit = NullableField<std::string>::value(j["unit"].get<std::string>());
        }
    }
    if (j.contains("validators")) {
        if (j["validators"].is_null()) {
            v.validators = NullableField<std::vector<RunCreateMeasurementsAggregationsValidators>>::make_null();
        } else {
            v.validators = NullableField<std::vector<RunCreateMeasurementsAggregationsValidators>>::value(j["validators"].get<std::vector<RunCreateMeasurementsAggregationsValidators>>());
        }
    }
}

/// Builder for RunCreateMeasurementsAggregations.
class RunCreateMeasurementsAggregationsBuilder {
public:
    /// Set the `type` field.
    /// Aggregation function: "min", "max", "avg", "sum", "count", "std", "median", "percentile_95", etc.
    RunCreateMeasurementsAggregationsBuilder& type(std::string value) {
        type_ = std::move(value);
        return *this;
    }

    /// Set the `outcome` field.
    /// Computed result of aggregation validation. Server stores as-is.
    RunCreateMeasurementsAggregationsBuilder& outcome(std::string value) {
        outcome_ = NullableField<std::string>::value(std::move(value));
        return *this;
    }

    /// Explicitly set `outcome` to null.
    RunCreateMeasurementsAggregationsBuilder& outcome_null() {
        outcome_ = NullableField<std::string>::make_null();
        return *this;
    }

    /// Set the `value` field.
    /// Computed aggregation value.
    RunCreateMeasurementsAggregationsBuilder& value(nlohmann::json value) {
        value_ = NullableField<nlohmann::json>::value(std::move(value));
        return *this;
    }

    /// Explicitly set `value` to null.
    RunCreateMeasurementsAggregationsBuilder& value_null() {
        value_ = NullableField<nlohmann::json>::make_null();
        return *this;
    }

    /// Set the `unit` field.
    /// Unit for the aggregated value.
    RunCreateMeasurementsAggregationsBuilder& unit(std::string value) {
        unit_ = NullableField<std::string>::value(std::move(value));
        return *this;
    }

    /// Explicitly set `unit` to null.
    RunCreateMeasurementsAggregationsBuilder& unit_null() {
        unit_ = NullableField<std::string>::make_null();
        return *this;
    }

    /// Set the `validators` field.
    /// Validators applied to the aggregated value.
    RunCreateMeasurementsAggregationsBuilder& validators(std::vector<RunCreateMeasurementsAggregationsValidators> value) {
        validators_ = NullableField<std::vector<RunCreateMeasurementsAggregationsValidators>>::value(std::move(value));
        return *this;
    }

    /// Explicitly set `validators` to null.
    RunCreateMeasurementsAggregationsBuilder& validators_null() {
        validators_ = NullableField<std::vector<RunCreateMeasurementsAggregationsValidators>>::make_null();
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    RunCreateMeasurementsAggregations build() const& {
        RunCreateMeasurementsAggregations result;
        if (!type_.has_value()) {
            throw std::runtime_error("missing required field: type");
        }
        result.type = type_.value();
        result.outcome = outcome_;
        result.value = value_;
        result.unit = unit_;
        result.validators = validators_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    RunCreateMeasurementsAggregations build() && {
        RunCreateMeasurementsAggregations result;
        if (!type_.has_value()) {
            throw std::runtime_error("missing required field: type");
        }
        result.type = std::move(type_.value());
        result.outcome = std::move(outcome_);
        result.value = std::move(value_);
        result.unit = std::move(unit_);
        result.validators = std::move(validators_);
        return result;
    }

private:
    std::optional<std::string> type_;
    NullableField<std::string> outcome_;
    NullableField<nlohmann::json> value_;
    NullableField<std::string> unit_;
    NullableField<std::vector<RunCreateMeasurementsAggregationsValidators>> validators_;
};

} // namespace tofupilot
