// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>


namespace tofupilot {

struct ImportTabularColumns {
    std::string column;
    std::optional<std::string> name;
    std::optional<std::string> units;
    std::optional<double> lower_limit;
    std::optional<double> upper_limit;
    std::optional<std::string> phase;
};

inline void to_json(nlohmann::json& j, const ImportTabularColumns& v) {
    j = nlohmann::json::object();
    j["column"] = v.column;
    if (v.name.has_value()) {
        j["name"] = v.name.value();
    }
    if (v.units.has_value()) {
        j["units"] = v.units.value();
    }
    if (v.lower_limit.has_value()) {
        j["lowerLimit"] = v.lower_limit.value();
    }
    if (v.upper_limit.has_value()) {
        j["upperLimit"] = v.upper_limit.value();
    }
    if (v.phase.has_value()) {
        j["phase"] = v.phase.value();
    }
}

inline void from_json(const nlohmann::json& j, ImportTabularColumns& v) {
    if (!j.contains("column")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: column", &j);
    }
    v.column = j["column"].get<std::string>();
    if (j.contains("name") && !j["name"].is_null()) {
        v.name = j["name"].get<std::string>();
    }
    if (j.contains("units") && !j["units"].is_null()) {
        v.units = j["units"].get<std::string>();
    }
    if (j.contains("lowerLimit") && !j["lowerLimit"].is_null()) {
        v.lower_limit = j["lowerLimit"].get<double>();
    }
    if (j.contains("upperLimit") && !j["upperLimit"].is_null()) {
        v.upper_limit = j["upperLimit"].get<double>();
    }
    if (j.contains("phase") && !j["phase"].is_null()) {
        v.phase = j["phase"].get<std::string>();
    }
}

/// Builder for ImportTabularColumns.
class ImportTabularColumnsBuilder {
public:
    /// Set the `column` field.
    ImportTabularColumnsBuilder& column(std::string value) {
        column_ = std::move(value);
        return *this;
    }

    /// Set the `name` field.
    ImportTabularColumnsBuilder& name(std::string value) {
        name_ = std::move(value);
        return *this;
    }

    /// Set the `units` field.
    ImportTabularColumnsBuilder& units(std::string value) {
        units_ = std::move(value);
        return *this;
    }

    /// Set the `lowerLimit` field.
    ImportTabularColumnsBuilder& lower_limit(double value) {
        lower_limit_ = std::move(value);
        return *this;
    }

    /// Set the `upperLimit` field.
    ImportTabularColumnsBuilder& upper_limit(double value) {
        upper_limit_ = std::move(value);
        return *this;
    }

    /// Set the `phase` field.
    ImportTabularColumnsBuilder& phase(std::string value) {
        phase_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    ImportTabularColumns build() const& {
        ImportTabularColumns result;
        if (!column_.has_value()) {
            throw std::runtime_error("missing required field: column");
        }
        result.column = column_.value();
        result.name = name_;
        result.units = units_;
        result.lower_limit = lower_limit_;
        result.upper_limit = upper_limit_;
        result.phase = phase_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    ImportTabularColumns build() && {
        ImportTabularColumns result;
        if (!column_.has_value()) {
            throw std::runtime_error("missing required field: column");
        }
        result.column = std::move(column_.value());
        result.name = std::move(name_);
        result.units = std::move(units_);
        result.lower_limit = std::move(lower_limit_);
        result.upper_limit = std::move(upper_limit_);
        result.phase = std::move(phase_);
        return result;
    }

private:
    std::optional<std::string> column_;
    std::optional<std::string> name_;
    std::optional<std::string> units_;
    std::optional<double> lower_limit_;
    std::optional<double> upper_limit_;
    std::optional<std::string> phase_;
};

} // namespace tofupilot
