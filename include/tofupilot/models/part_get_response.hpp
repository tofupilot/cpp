// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/nullable.hpp"
#include "tofupilot/models/part_get_created_by_station.hpp"
#include "tofupilot/models/part_get_created_by_user.hpp"
#include "tofupilot/models/part_get_revisions.hpp"

namespace tofupilot {

/// Part retrieved successfully
struct PartGetResponse {
    /// Unique identifier for the part.
    std::string id;
    /// Part number.
    std::string number;
    /// Part name.
    std::string name;
    /// ISO 8601 timestamp when the part was created.
    std::string created_at;
    /// User who created this part.
    NullableField<PartGetCreatedByUser> created_by_user;
    /// Station that created this part.
    NullableField<PartGetCreatedByStation> created_by_station;
    /// List of revisions for this part.
    std::vector<PartGetRevisions> revisions;
};

inline void to_json(nlohmann::json& j, const PartGetResponse& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    j["number"] = v.number;
    j["name"] = v.name;
    j["created_at"] = v.created_at;
    if (!v.created_by_user.is_absent()) {
        if (v.created_by_user.is_null()) {
            j["created_by_user"] = nullptr;
        } else {
            j["created_by_user"] = v.created_by_user.get();
        }
    }
    if (!v.created_by_station.is_absent()) {
        if (v.created_by_station.is_null()) {
            j["created_by_station"] = nullptr;
        } else {
            j["created_by_station"] = v.created_by_station.get();
        }
    }
    j["revisions"] = v.revisions;
}

inline void from_json(const nlohmann::json& j, PartGetResponse& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (!j.contains("number")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: number", &j);
    }
    v.number = j["number"].get<std::string>();
    if (!j.contains("name")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: name", &j);
    }
    v.name = j["name"].get<std::string>();
    if (!j.contains("created_at")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: created_at", &j);
    }
    v.created_at = j["created_at"].get<std::string>();
    if (j.contains("created_by_user")) {
        if (j["created_by_user"].is_null()) {
            v.created_by_user = NullableField<PartGetCreatedByUser>::make_null();
        } else {
            v.created_by_user = NullableField<PartGetCreatedByUser>::value(j["created_by_user"].get<PartGetCreatedByUser>());
        }
    }
    if (j.contains("created_by_station")) {
        if (j["created_by_station"].is_null()) {
            v.created_by_station = NullableField<PartGetCreatedByStation>::make_null();
        } else {
            v.created_by_station = NullableField<PartGetCreatedByStation>::value(j["created_by_station"].get<PartGetCreatedByStation>());
        }
    }
    if (j.contains("revisions") && j["revisions"].is_array()) {
        v.revisions = j["revisions"].get<std::vector<PartGetRevisions>>();
    }
}

} // namespace tofupilot
