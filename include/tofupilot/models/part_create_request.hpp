// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>


namespace tofupilot {

struct PartCreateRequest {
    /// Unique identifier number for the part.
    std::string number;
    /// Human-readable name for the part. If not provided, a default name will be used.
    std::optional<std::string> name;
    /// Revision identifier for the part version. If not provided, default revision identifier will be used.
    std::optional<std::string> revision_number;
};

inline void to_json(nlohmann::json& j, const PartCreateRequest& v) {
    j = nlohmann::json::object();
    j["number"] = v.number;
    if (v.name.has_value()) {
        j["name"] = v.name.value();
    }
    if (v.revision_number.has_value()) {
        j["revision_number"] = v.revision_number.value();
    }
}

inline void from_json(const nlohmann::json& j, PartCreateRequest& v) {
    if (!j.contains("number")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: number", &j);
    }
    v.number = j["number"].get<std::string>();
    if (j.contains("name") && !j["name"].is_null()) {
        v.name = j["name"].get<std::string>();
    }
    if (j.contains("revision_number") && !j["revision_number"].is_null()) {
        v.revision_number = j["revision_number"].get<std::string>();
    }
}

/// Builder for PartCreateRequest.
class PartCreateRequestBuilder {
public:
    /// Set the `number` field.
    /// Unique identifier number for the part.
    PartCreateRequestBuilder& number(std::string value) {
        number_ = std::move(value);
        return *this;
    }

    /// Set the `name` field.
    /// Human-readable name for the part. If not provided, a default name will be used.
    PartCreateRequestBuilder& name(std::string value) {
        name_ = std::move(value);
        return *this;
    }

    /// Set the `revision_number` field.
    /// Revision identifier for the part version. If not provided, default revision identifier will be used.
    PartCreateRequestBuilder& revision_number(std::string value) {
        revision_number_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    PartCreateRequest build() const& {
        PartCreateRequest result;
        if (!number_.has_value()) {
            throw std::runtime_error("missing required field: number");
        }
        result.number = number_.value();
        result.name = name_;
        result.revision_number = revision_number_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    PartCreateRequest build() && {
        PartCreateRequest result;
        if (!number_.has_value()) {
            throw std::runtime_error("missing required field: number");
        }
        result.number = std::move(number_.value());
        result.name = std::move(name_);
        result.revision_number = std::move(revision_number_);
        return result;
    }

private:
    std::optional<std::string> number_;
    std::optional<std::string> name_;
    std::optional<std::string> revision_number_;
};

} // namespace tofupilot
