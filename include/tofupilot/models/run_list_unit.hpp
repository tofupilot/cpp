// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/nullable.hpp"
#include "tofupilot/models/run_list_batch.hpp"
#include "tofupilot/models/run_list_part.hpp"

namespace tofupilot {

/// Unit under test information.
struct RunListUnit {
    /// Unit ID.
    std::string id;
    /// Unit serial number.
    std::string serial_number;
    /// Part information with revision details.
    RunListPart part;
    /// Batch information for this unit.
    NullableField<RunListBatch> batch;
};

inline void to_json(nlohmann::json& j, const RunListUnit& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    j["serial_number"] = v.serial_number;
    j["part"] = v.part;
    if (!v.batch.is_absent()) {
        if (v.batch.is_null()) {
            j["batch"] = nullptr;
        } else {
            j["batch"] = v.batch.get();
        }
    }
}

inline void from_json(const nlohmann::json& j, RunListUnit& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (!j.contains("serial_number")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: serial_number", &j);
    }
    v.serial_number = j["serial_number"].get<std::string>();
    if (!j.contains("part")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: part", &j);
    }
    v.part = j["part"].get<RunListPart>();
    if (j.contains("batch")) {
        if (j["batch"].is_null()) {
            v.batch = NullableField<RunListBatch>::make_null();
        } else {
            v.batch = NullableField<RunListBatch>::value(j["batch"].get<RunListBatch>());
        }
    }
}

/// Builder for RunListUnit.
class RunListUnitBuilder {
public:
    /// Set the `id` field.
    /// Unit ID.
    RunListUnitBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    /// Set the `serial_number` field.
    /// Unit serial number.
    RunListUnitBuilder& serial_number(std::string value) {
        serial_number_ = std::move(value);
        return *this;
    }

    /// Set the `part` field.
    /// Part information with revision details.
    RunListUnitBuilder& part(RunListPart value) {
        part_ = std::move(value);
        return *this;
    }

    /// Set the `batch` field.
    /// Batch information for this unit.
    RunListUnitBuilder& batch(RunListBatch value) {
        batch_ = NullableField<RunListBatch>::value(std::move(value));
        return *this;
    }

    /// Explicitly set `batch` to null.
    RunListUnitBuilder& batch_null() {
        batch_ = NullableField<RunListBatch>::make_null();
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    RunListUnit build() const& {
        RunListUnit result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = id_.value();
        if (!serial_number_.has_value()) {
            throw std::runtime_error("missing required field: serial_number");
        }
        result.serial_number = serial_number_.value();
        if (!part_.has_value()) {
            throw std::runtime_error("missing required field: part");
        }
        result.part = part_.value();
        result.batch = batch_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    RunListUnit build() && {
        RunListUnit result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = std::move(id_.value());
        if (!serial_number_.has_value()) {
            throw std::runtime_error("missing required field: serial_number");
        }
        result.serial_number = std::move(serial_number_.value());
        if (!part_.has_value()) {
            throw std::runtime_error("missing required field: part");
        }
        result.part = std::move(part_.value());
        result.batch = std::move(batch_);
        return result;
    }

private:
    std::optional<std::string> id_;
    std::optional<std::string> serial_number_;
    std::optional<RunListPart> part_;
    NullableField<RunListBatch> batch_;
};

} // namespace tofupilot
