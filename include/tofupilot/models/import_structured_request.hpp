// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/import_structured_items.hpp"

namespace tofupilot {

struct ImportStructuredRequest {
    /// Files to import (1–100). Pass a single-item list to import one file. Each item is parsed independently; one failure does not abort the others.
    std::vector<ImportStructuredItems> items;
};

inline void to_json(nlohmann::json& j, const ImportStructuredRequest& v) {
    j = nlohmann::json::object();
    j["items"] = v.items;
}

inline void from_json(const nlohmann::json& j, ImportStructuredRequest& v) {
    if (j.contains("items") && j["items"].is_array()) {
        v.items = j["items"].get<std::vector<ImportStructuredItems>>();
    }
}

/// Builder for ImportStructuredRequest.
class ImportStructuredRequestBuilder {
public:
    /// Set the `items` field.
    /// Files to import (1–100). Pass a single-item list to import one file. Each item is parsed independently; one failure does not abort the others.
    ImportStructuredRequestBuilder& items(std::vector<ImportStructuredItems> value) {
        items_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    ImportStructuredRequest build() const& {
        ImportStructuredRequest result;
        if (items_.has_value()) {
            result.items = items_.value();
        }
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    ImportStructuredRequest build() && {
        ImportStructuredRequest result;
        if (items_.has_value()) {
            result.items = std::move(items_.value());
        }
        return result;
    }

private:
    std::optional<std::vector<ImportStructuredItems>> items_;
};

} // namespace tofupilot
