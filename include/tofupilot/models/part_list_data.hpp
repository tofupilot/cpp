// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/part_list_revisions.hpp"

namespace tofupilot {

struct PartListData {
    /// Unique database identifier of the part.
    std::string id;
    /// Unique part number identifier.
    std::string number;
    /// Human-readable name of the part.
    std::string name;
    /// Time at which the part was created.
    std::string created_at;
    /// List of revisions for this part.
    std::vector<PartListRevisions> revisions;
};

inline void to_json(nlohmann::json& j, const PartListData& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    j["number"] = v.number;
    j["name"] = v.name;
    j["created_at"] = v.created_at;
    j["revisions"] = v.revisions;
}

inline void from_json(const nlohmann::json& j, PartListData& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (!j.contains("number")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: number", &j);
    }
    v.number = j["number"].get<std::string>();
    if (!j.contains("name")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: name", &j);
    }
    v.name = j["name"].get<std::string>();
    if (!j.contains("created_at")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: created_at", &j);
    }
    v.created_at = j["created_at"].get<std::string>();
    if (j.contains("revisions") && j["revisions"].is_array()) {
        v.revisions = j["revisions"].get<std::vector<PartListRevisions>>();
    }
}

/// Builder for PartListData.
class PartListDataBuilder {
public:
    /// Set the `id` field.
    /// Unique database identifier of the part.
    PartListDataBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    /// Set the `number` field.
    /// Unique part number identifier.
    PartListDataBuilder& number(std::string value) {
        number_ = std::move(value);
        return *this;
    }

    /// Set the `name` field.
    /// Human-readable name of the part.
    PartListDataBuilder& name(std::string value) {
        name_ = std::move(value);
        return *this;
    }

    /// Set the `created_at` field.
    /// Time at which the part was created.
    PartListDataBuilder& created_at(std::string value) {
        created_at_ = std::move(value);
        return *this;
    }

    /// Set the `revisions` field.
    /// List of revisions for this part.
    PartListDataBuilder& revisions(std::vector<PartListRevisions> value) {
        revisions_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    PartListData build() const& {
        PartListData result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = id_.value();
        if (!number_.has_value()) {
            throw std::runtime_error("missing required field: number");
        }
        result.number = number_.value();
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = name_.value();
        if (!created_at_.has_value()) {
            throw std::runtime_error("missing required field: created_at");
        }
        result.created_at = created_at_.value();
        if (revisions_.has_value()) {
            result.revisions = revisions_.value();
        }
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    PartListData build() && {
        PartListData result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = std::move(id_.value());
        if (!number_.has_value()) {
            throw std::runtime_error("missing required field: number");
        }
        result.number = std::move(number_.value());
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = std::move(name_.value());
        if (!created_at_.has_value()) {
            throw std::runtime_error("missing required field: created_at");
        }
        result.created_at = std::move(created_at_.value());
        if (revisions_.has_value()) {
            result.revisions = std::move(revisions_.value());
        }
        return result;
    }

private:
    std::optional<std::string> id_;
    std::optional<std::string> number_;
    std::optional<std::string> name_;
    std::optional<std::string> created_at_;
    std::optional<std::vector<PartListRevisions>> revisions_;
};

} // namespace tofupilot
