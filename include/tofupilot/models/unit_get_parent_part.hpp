// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/unit_get_parent_part_revision.hpp"

namespace tofupilot {

struct UnitGetParentPart {
    /// Part ID.
    std::string id;
    /// Part number.
    std::string number;
    /// Part name.
    std::string name;
    /// Part revision information.
    std::optional<UnitGetParentPartRevision> revision;
};

inline void to_json(nlohmann::json& j, const UnitGetParentPart& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    j["number"] = v.number;
    j["name"] = v.name;
    if (v.revision.has_value()) {
        j["revision"] = v.revision.value();
    }
}

inline void from_json(const nlohmann::json& j, UnitGetParentPart& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (!j.contains("number")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: number", &j);
    }
    v.number = j["number"].get<std::string>();
    if (!j.contains("name")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: name", &j);
    }
    v.name = j["name"].get<std::string>();
    if (j.contains("revision") && !j["revision"].is_null()) {
        v.revision = j["revision"].get<UnitGetParentPartRevision>();
    }
}

/// Builder for UnitGetParentPart.
class UnitGetParentPartBuilder {
public:
    /// Set the `id` field.
    /// Part ID.
    UnitGetParentPartBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    /// Set the `number` field.
    /// Part number.
    UnitGetParentPartBuilder& number(std::string value) {
        number_ = std::move(value);
        return *this;
    }

    /// Set the `name` field.
    /// Part name.
    UnitGetParentPartBuilder& name(std::string value) {
        name_ = std::move(value);
        return *this;
    }

    /// Set the `revision` field.
    /// Part revision information.
    UnitGetParentPartBuilder& revision(UnitGetParentPartRevision value) {
        revision_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    UnitGetParentPart build() const& {
        UnitGetParentPart result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = id_.value();
        if (!number_.has_value()) {
            throw std::runtime_error("missing required field: number");
        }
        result.number = number_.value();
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = name_.value();
        result.revision = revision_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    UnitGetParentPart build() && {
        UnitGetParentPart result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = std::move(id_.value());
        if (!number_.has_value()) {
            throw std::runtime_error("missing required field: number");
        }
        result.number = std::move(number_.value());
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = std::move(name_.value());
        result.revision = std::move(revision_);
        return result;
    }

private:
    std::optional<std::string> id_;
    std::optional<std::string> number_;
    std::optional<std::string> name_;
    std::optional<UnitGetParentPartRevision> revision_;
};

} // namespace tofupilot
