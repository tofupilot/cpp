// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/log_get_outcome.hpp"

namespace tofupilot {

struct ImportTabularOutcome1 {
    std::string column;
    std::optional<std::map<std::string, LogGetOutcome>> value_map;
};

inline void to_json(nlohmann::json& j, const ImportTabularOutcome1& v) {
    j = nlohmann::json::object();
    j["column"] = v.column;
    if (v.value_map.has_value()) {
        j["valueMap"] = v.value_map.value();
    }
}

inline void from_json(const nlohmann::json& j, ImportTabularOutcome1& v) {
    if (!j.contains("column")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: column", &j);
    }
    v.column = j["column"].get<std::string>();
    if (j.contains("valueMap") && !j["valueMap"].is_null()) {
        v.value_map = j["valueMap"].get<std::map<std::string, LogGetOutcome>>();
    }
}

/// Builder for ImportTabularOutcome1.
class ImportTabularOutcome1Builder {
public:
    /// Set the `column` field.
    ImportTabularOutcome1Builder& column(std::string value) {
        column_ = std::move(value);
        return *this;
    }

    /// Set the `valueMap` field.
    ImportTabularOutcome1Builder& value_map(std::map<std::string, LogGetOutcome> value) {
        value_map_ = std::move(value);
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    ImportTabularOutcome1 build() const& {
        ImportTabularOutcome1 result;
        if (!column_.has_value()) {
            throw std::runtime_error("missing required field: column");
        }
        result.column = column_.value();
        result.value_map = value_map_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    ImportTabularOutcome1 build() && {
        ImportTabularOutcome1 result;
        if (!column_.has_value()) {
            throw std::runtime_error("missing required field: column");
        }
        result.column = std::move(column_.value());
        result.value_map = std::move(value_map_);
        return result;
    }

private:
    std::optional<std::string> column_;
    std::optional<std::map<std::string, LogGetOutcome>> value_map_;
};

} // namespace tofupilot
