// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/nullable.hpp"
#include "tofupilot/models/unit_get_attachments.hpp"
#include "tofupilot/models/unit_get_batch.hpp"
#include "tofupilot/models/unit_get_children.hpp"
#include "tofupilot/models/unit_get_created_by_station.hpp"
#include "tofupilot/models/unit_get_created_by_user.hpp"
#include "tofupilot/models/unit_get_created_during.hpp"
#include "tofupilot/models/unit_get_parent.hpp"
#include "tofupilot/models/unit_get_part.hpp"

namespace tofupilot {

/// Units retrieved successfully
struct UnitGetResponse {
    /// Unique identifier for the unit.
    std::string id;
    /// Unit serial number.
    std::string serial_number;
    /// ISO 8601 timestamp when the unit was created.
    std::string created_at;
    /// User who created this unit.
    NullableField<UnitGetCreatedByUser> created_by_user;
    /// Station that created this unit.
    NullableField<UnitGetCreatedByStation> created_by_station;
    /// Part information with revision details for this unit. Every unit must have a part and revision.
    UnitGetPart part;
    /// Batch information for this unit.
    NullableField<UnitGetBatch> batch;
    /// Parent unit information with part details and processed images.
    std::optional<UnitGetParent> parent;
    /// Child units with part details.
    std::optional<std::vector<UnitGetChildren>> children;
    /// Run that created this unit.
    NullableField<UnitGetCreatedDuring> created_during;
    /// Files attached to this unit.
    std::optional<std::vector<UnitGetAttachments>> attachments;
};

inline void to_json(nlohmann::json& j, const UnitGetResponse& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    j["serial_number"] = v.serial_number;
    j["created_at"] = v.created_at;
    if (!v.created_by_user.is_absent()) {
        if (v.created_by_user.is_null()) {
            j["created_by_user"] = nullptr;
        } else {
            j["created_by_user"] = v.created_by_user.get();
        }
    }
    if (!v.created_by_station.is_absent()) {
        if (v.created_by_station.is_null()) {
            j["created_by_station"] = nullptr;
        } else {
            j["created_by_station"] = v.created_by_station.get();
        }
    }
    j["part"] = v.part;
    if (!v.batch.is_absent()) {
        if (v.batch.is_null()) {
            j["batch"] = nullptr;
        } else {
            j["batch"] = v.batch.get();
        }
    }
    if (v.parent.has_value()) {
        j["parent"] = v.parent.value();
    }
    if (v.children.has_value()) {
        j["children"] = v.children.value();
    }
    if (!v.created_during.is_absent()) {
        if (v.created_during.is_null()) {
            j["created_during"] = nullptr;
        } else {
            j["created_during"] = v.created_during.get();
        }
    }
    if (v.attachments.has_value()) {
        j["attachments"] = v.attachments.value();
    }
}

inline void from_json(const nlohmann::json& j, UnitGetResponse& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (!j.contains("serial_number")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: serial_number", &j);
    }
    v.serial_number = j["serial_number"].get<std::string>();
    if (!j.contains("created_at")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: created_at", &j);
    }
    v.created_at = j["created_at"].get<std::string>();
    if (j.contains("created_by_user")) {
        if (j["created_by_user"].is_null()) {
            v.created_by_user = NullableField<UnitGetCreatedByUser>::make_null();
        } else {
            v.created_by_user = NullableField<UnitGetCreatedByUser>::value(j["created_by_user"].get<UnitGetCreatedByUser>());
        }
    }
    if (j.contains("created_by_station")) {
        if (j["created_by_station"].is_null()) {
            v.created_by_station = NullableField<UnitGetCreatedByStation>::make_null();
        } else {
            v.created_by_station = NullableField<UnitGetCreatedByStation>::value(j["created_by_station"].get<UnitGetCreatedByStation>());
        }
    }
    if (!j.contains("part")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: part", &j);
    }
    v.part = j["part"].get<UnitGetPart>();
    if (j.contains("batch")) {
        if (j["batch"].is_null()) {
            v.batch = NullableField<UnitGetBatch>::make_null();
        } else {
            v.batch = NullableField<UnitGetBatch>::value(j["batch"].get<UnitGetBatch>());
        }
    }
    if (j.contains("parent") && !j["parent"].is_null()) {
        v.parent = j["parent"].get<UnitGetParent>();
    }
    if (j.contains("children") && !j["children"].is_null()) {
        v.children = j["children"].get<std::vector<UnitGetChildren>>();
    }
    if (j.contains("created_during")) {
        if (j["created_during"].is_null()) {
            v.created_during = NullableField<UnitGetCreatedDuring>::make_null();
        } else {
            v.created_during = NullableField<UnitGetCreatedDuring>::value(j["created_during"].get<UnitGetCreatedDuring>());
        }
    }
    if (j.contains("attachments") && !j["attachments"].is_null()) {
        v.attachments = j["attachments"].get<std::vector<UnitGetAttachments>>();
    }
}

} // namespace tofupilot
