// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/nullable.hpp"

namespace tofupilot {

struct StationUpdateRequestBody {
    /// New name for the station
    std::optional<std::string> name;
    /// Upload ID for the station image, or empty string to remove image
    std::optional<std::string> image_id;
    /// Team ID to assign this station to, or null to unassign
    NullableField<std::string> team_id;
};

inline void to_json(nlohmann::json& j, const StationUpdateRequestBody& v) {
    j = nlohmann::json::object();
    if (v.name.has_value()) {
        j["name"] = v.name.value();
    }
    if (v.image_id.has_value()) {
        j["image_id"] = v.image_id.value();
    }
    if (!v.team_id.is_absent()) {
        if (v.team_id.is_null()) {
            j["team_id"] = nullptr;
        } else {
            j["team_id"] = v.team_id.get();
        }
    }
}

inline void from_json(const nlohmann::json& j, StationUpdateRequestBody& v) {
    if (j.contains("name") && !j["name"].is_null()) {
        v.name = j["name"].get<std::string>();
    }
    if (j.contains("image_id") && !j["image_id"].is_null()) {
        v.image_id = j["image_id"].get<std::string>();
    }
    if (j.contains("team_id")) {
        if (j["team_id"].is_null()) {
            v.team_id = NullableField<std::string>::make_null();
        } else {
            v.team_id = NullableField<std::string>::value(j["team_id"].get<std::string>());
        }
    }
}

/// Builder for StationUpdateRequestBody.
class StationUpdateRequestBodyBuilder {
public:
    /// Set the `name` field.
    /// New name for the station
    StationUpdateRequestBodyBuilder& name(std::string value) {
        name_ = std::move(value);
        return *this;
    }

    /// Set the `image_id` field.
    /// Upload ID for the station image, or empty string to remove image
    StationUpdateRequestBodyBuilder& image_id(std::string value) {
        image_id_ = std::move(value);
        return *this;
    }

    /// Set the `team_id` field.
    /// Team ID to assign this station to, or null to unassign
    StationUpdateRequestBodyBuilder& team_id(std::string value) {
        team_id_ = NullableField<std::string>::value(std::move(value));
        return *this;
    }

    /// Explicitly set `team_id` to null.
    StationUpdateRequestBodyBuilder& team_id_null() {
        team_id_ = NullableField<std::string>::make_null();
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    StationUpdateRequestBody build() const& {
        StationUpdateRequestBody result;
        result.name = name_;
        result.image_id = image_id_;
        result.team_id = team_id_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    StationUpdateRequestBody build() && {
        StationUpdateRequestBody result;
        result.name = std::move(name_);
        result.image_id = std::move(image_id_);
        result.team_id = std::move(team_id_);
        return result;
    }

private:
    std::optional<std::string> name_;
    std::optional<std::string> image_id_;
    NullableField<std::string> team_id_;
};

} // namespace tofupilot
