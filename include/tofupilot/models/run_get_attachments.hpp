// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/nullable.hpp"

namespace tofupilot {

struct RunGetAttachments {
    /// Attachment ID.
    std::string id;
    /// File name.
    std::string name;
    /// File size in bytes.
    std::optional<double> size;
    /// MIME type of the file.
    std::optional<std::string> content_type;
    /// Whether this attachment is a test report (from file import) or a regular attachment.
    bool is_report;
    /// Presigned URL for downloading the file. This URL is temporary and will expire.
    NullableField<std::string> download_url;
};

inline void to_json(nlohmann::json& j, const RunGetAttachments& v) {
    j = nlohmann::json::object();
    j["id"] = v.id;
    j["name"] = v.name;
    if (v.size.has_value()) {
        j["size"] = v.size.value();
    }
    if (v.content_type.has_value()) {
        j["content_type"] = v.content_type.value();
    }
    j["is_report"] = v.is_report;
    if (!v.download_url.is_absent()) {
        if (v.download_url.is_null()) {
            j["download_url"] = nullptr;
        } else {
            j["download_url"] = v.download_url.get();
        }
    }
}

inline void from_json(const nlohmann::json& j, RunGetAttachments& v) {
    if (!j.contains("id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: id", &j);
    }
    v.id = j["id"].get<std::string>();
    if (!j.contains("name")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: name", &j);
    }
    v.name = j["name"].get<std::string>();
    if (j.contains("size") && !j["size"].is_null()) {
        v.size = j["size"].get<double>();
    }
    if (j.contains("content_type") && !j["content_type"].is_null()) {
        v.content_type = j["content_type"].get<std::string>();
    }
    if (!j.contains("is_report")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: is_report", &j);
    }
    v.is_report = j["is_report"].get<bool>();
    if (j.contains("download_url")) {
        if (j["download_url"].is_null()) {
            v.download_url = NullableField<std::string>::make_null();
        } else {
            v.download_url = NullableField<std::string>::value(j["download_url"].get<std::string>());
        }
    }
}

/// Builder for RunGetAttachments.
class RunGetAttachmentsBuilder {
public:
    /// Set the `id` field.
    /// Attachment ID.
    RunGetAttachmentsBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    /// Set the `name` field.
    /// File name.
    RunGetAttachmentsBuilder& name(std::string value) {
        name_ = std::move(value);
        return *this;
    }

    /// Set the `size` field.
    /// File size in bytes.
    RunGetAttachmentsBuilder& size(double value) {
        size_ = std::move(value);
        return *this;
    }

    /// Set the `content_type` field.
    /// MIME type of the file.
    RunGetAttachmentsBuilder& content_type(std::string value) {
        content_type_ = std::move(value);
        return *this;
    }

    /// Set the `is_report` field.
    /// Whether this attachment is a test report (from file import) or a regular attachment.
    RunGetAttachmentsBuilder& is_report(bool value) {
        is_report_ = std::move(value);
        return *this;
    }

    /// Set the `download_url` field.
    /// Presigned URL for downloading the file. This URL is temporary and will expire.
    RunGetAttachmentsBuilder& download_url(std::string value) {
        download_url_ = NullableField<std::string>::value(std::move(value));
        return *this;
    }

    /// Explicitly set `download_url` to null.
    RunGetAttachmentsBuilder& download_url_null() {
        download_url_ = NullableField<std::string>::make_null();
        return *this;
    }

    /// Build the struct. Throws std::runtime_error if required fields are missing.
    RunGetAttachments build() const& {
        RunGetAttachments result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = id_.value();
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = name_.value();
        result.size = size_;
        result.content_type = content_type_;
        if (!is_report_.has_value()) {
            throw std::runtime_error("missing required field: is_report");
        }
        result.is_report = is_report_.value();
        result.download_url = download_url_;
        return result;
    }

    /// Build the struct (move overload). Throws std::runtime_error if required fields are missing.
    RunGetAttachments build() && {
        RunGetAttachments result;
        if (!id_.has_value()) {
            throw std::runtime_error("missing required field: id");
        }
        result.id = std::move(id_.value());
        if (!name_.has_value()) {
            throw std::runtime_error("missing required field: name");
        }
        result.name = std::move(name_.value());
        result.size = std::move(size_);
        result.content_type = std::move(content_type_);
        if (!is_report_.has_value()) {
            throw std::runtime_error("missing required field: is_report");
        }
        result.is_report = std::move(is_report_.value());
        result.download_url = std::move(download_url_);
        return result;
    }

private:
    std::optional<std::string> id_;
    std::optional<std::string> name_;
    std::optional<double> size_;
    std::optional<std::string> content_type_;
    std::optional<bool> is_report_;
    NullableField<std::string> download_url_;
};

} // namespace tofupilot
