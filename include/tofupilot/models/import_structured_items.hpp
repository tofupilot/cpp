// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <stdexcept>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "tofupilot/models/import_structured_importer.hpp"

namespace tofupilot {

struct ImportStructuredItems {
    /// ID of a previously uploaded file (via Initialize and Finalize upload).
    std::string upload_id;
    /// Source format of the uploaded file. OPENHTF for OpenHTF JSON logs; WATS for Virinco WATS WSJF (JSON); WSXF for WATS WSXF (XML); ATML for IEEE 1671 ATML Test Results (XML); TESTSTAND for NI TestStand native XML reports; STDF for binary STDF V4 datalogs; ATDF for ATDF (the ASCII text form of STDF). For CSV/tabular files use the dedicated tabular import endpoint.
    ImportStructuredImporter importer;
};

inline void to_json(nlohmann::json& j, const ImportStructuredItems& v) {
    j = nlohmann::json::object();
    j["upload_id"] = v.upload_id;
    j["importer"] = v.importer;
}

inline void from_json(const nlohmann::json& j, ImportStructuredItems& v) {
    if (!j.contains("upload_id")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: upload_id", &j);
    }
    v.upload_id = j["upload_id"].get<std::string>();
    if (!j.contains("importer")) {
        throw nlohmann::json::other_error::create(501,
            "missing required field in response: importer", &j);
    }
    v.importer = j["importer"].get<ImportStructuredImporter>();
}

} // namespace tofupilot
