// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <chrono>
#include <optional>
#include <sstream>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "config.hpp"
#include "detail.hpp"
#include "error.hpp"
#include "types.hpp"

namespace tofupilot {

class TofuPilot;

class UserClient {
public:
    explicit UserClient(TofuPilot& client) noexcept : client_(client) {}

    class ListBuilder;
    ListBuilder list();

private:
    TofuPilot& client_;
};

class UserClient::ListBuilder {
public:
    explicit ListBuilder(TofuPilot& client) noexcept : client_(client) {}

    ListBuilder& current(bool value) {
        current_ = std::move(value);
        return *this;
    }

    ListBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    ListBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    std::vector<UserListResponse> send() {

        std::string path = "/v2/users";

        std::string query_string;
        {
            std::ostringstream qs;
            bool first = true;
            if (current_.has_value()) {
                if (!first) qs << "&";
                qs << "current=" << detail::url_encode(detail::to_query_string(current_.value()));
                first = false;
            }
            query_string = qs.str();
        }

        std::string body_str;
        std::string content_type;

        auto result = client_.execute("GET", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return {};
        }
        try {
            return nlohmann::json::parse(resp_body).get<std::vector<UserListResponse>>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<bool> current_;
    RequestConfig request_config_;
};

inline UserClient::ListBuilder UserClient::list() {
    return ListBuilder(client_);
}

} // namespace tofupilot
