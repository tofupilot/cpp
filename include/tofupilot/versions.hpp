// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <chrono>
#include <optional>
#include <sstream>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "config.hpp"
#include "detail.hpp"
#include "error.hpp"
#include "types.hpp"

namespace tofupilot {

class TofuPilot;

class VersionsClient {
public:
    explicit VersionsClient(TofuPilot& client) noexcept : client_(client) {}

    class GetBuilder;
    GetBuilder get();
    class DeleteBuilder;
    DeleteBuilder delete_();
    class CreateBuilder;
    CreateBuilder create();

private:
    TofuPilot& client_;
};

class VersionsClient::GetBuilder {
public:
    explicit GetBuilder(TofuPilot& client) noexcept : client_(client) {}

    GetBuilder& procedure_id(std::string value) {
        procedure_id_ = std::move(value);
        return *this;
    }
    GetBuilder& tag(std::string value) {
        tag_ = std::move(value);
        return *this;
    }

    GetBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    GetBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    ProcedureGetVersionResponse send() {
        if (!procedure_id_.has_value()) throw ValidationError("missing required path parameter: procedure_id");
        if (!tag_.has_value()) throw ValidationError("missing required path parameter: tag");

        std::string path = "/v2/procedures/" + detail::url_encode(procedure_id_.value()) + "/versions/" + detail::url_encode(tag_.value()) + "";

        std::string query_string;

        std::string body_str;
        std::string content_type;

        auto result = client_.execute("GET", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return ProcedureGetVersionResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<ProcedureGetVersionResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> procedure_id_;
    std::optional<std::string> tag_;
    RequestConfig request_config_;
};

class VersionsClient::DeleteBuilder {
public:
    explicit DeleteBuilder(TofuPilot& client) noexcept : client_(client) {}

    DeleteBuilder& procedure_id(std::string value) {
        procedure_id_ = std::move(value);
        return *this;
    }
    DeleteBuilder& tag(std::string value) {
        tag_ = std::move(value);
        return *this;
    }

    DeleteBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    DeleteBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    ProcedureDeleteVersionResponse send() {
        if (!procedure_id_.has_value()) throw ValidationError("missing required path parameter: procedure_id");
        if (!tag_.has_value()) throw ValidationError("missing required path parameter: tag");

        std::string path = "/v2/procedures/" + detail::url_encode(procedure_id_.value()) + "/versions/" + detail::url_encode(tag_.value()) + "";

        std::string query_string;

        std::string body_str;
        std::string content_type;

        auto result = client_.execute("DELETE", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return ProcedureDeleteVersionResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<ProcedureDeleteVersionResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> procedure_id_;
    std::optional<std::string> tag_;
    RequestConfig request_config_;
};

class VersionsClient::CreateBuilder {
public:
    explicit CreateBuilder(TofuPilot& client) noexcept : client_(client) {}

    CreateBuilder& procedure_id(std::string value) {
        procedure_id_ = std::move(value);
        return *this;
    }
    CreateBuilder& tag(std::string value) {
        tag_ = std::move(value);
        return *this;
    }
    CreateBuilder& body(ProcedureCreateVersionRequestBody b) {
        tag_ = std::move(b.tag);
        return *this;
    }

    CreateBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    CreateBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    ProcedureCreateVersionResponse send() {
        if (!procedure_id_.has_value()) throw ValidationError("missing required path parameter: procedure_id");

        std::string path = "/v2/procedures/" + detail::url_encode(procedure_id_.value()) + "/versions";

        std::string query_string;

        std::string body_str;
        std::string content_type;
        {
            ProcedureCreateVersionRequestBody req_body;
            if (!tag_.has_value()) throw ValidationError("missing required field: tag");
            req_body.tag = tag_.value();
            body_str = nlohmann::json(req_body).dump();
            content_type = "application/json";
        }

        auto result = client_.execute("POST", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return ProcedureCreateVersionResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<ProcedureCreateVersionResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> procedure_id_;
    std::optional<std::string> tag_;
    RequestConfig request_config_;
};

inline VersionsClient::GetBuilder VersionsClient::get() {
    return GetBuilder(client_);
}
inline VersionsClient::DeleteBuilder VersionsClient::delete_() {
    return DeleteBuilder(client_);
}
inline VersionsClient::CreateBuilder VersionsClient::create() {
    return CreateBuilder(client_);
}

} // namespace tofupilot
