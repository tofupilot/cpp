// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <chrono>
#include <optional>
#include <sstream>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "config.hpp"
#include "detail.hpp"
#include "error.hpp"
#include "types.hpp"

namespace tofupilot {

class TofuPilot;

class BatchesClient {
public:
    explicit BatchesClient(TofuPilot& client) noexcept : client_(client) {}

    class GetBuilder;
    GetBuilder get();
    class DeleteBuilder;
    DeleteBuilder delete_();
    class UpdateBuilder;
    UpdateBuilder update();
    class ListBuilder;
    ListBuilder list();
    class CreateBuilder;
    CreateBuilder create();

private:
    TofuPilot& client_;
};

class BatchesClient::GetBuilder {
public:
    explicit GetBuilder(TofuPilot& client) noexcept : client_(client) {}

    GetBuilder& number(std::string value) {
        number_ = std::move(value);
        return *this;
    }

    GetBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    GetBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    BatchGetResponse send() {
        if (!number_.has_value()) throw ValidationError("missing required path parameter: number");

        std::string path = "/v2/batches/" + detail::url_encode(number_.value()) + "";

        std::string query_string;

        std::string body_str;
        std::string content_type;

        auto result = client_.execute("GET", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return BatchGetResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<BatchGetResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> number_;
    RequestConfig request_config_;
};

class BatchesClient::DeleteBuilder {
public:
    explicit DeleteBuilder(TofuPilot& client) noexcept : client_(client) {}

    DeleteBuilder& number(std::string value) {
        number_ = std::move(value);
        return *this;
    }

    DeleteBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    DeleteBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    BatchDeleteResponse send() {
        if (!number_.has_value()) throw ValidationError("missing required path parameter: number");

        std::string path = "/v2/batches/" + detail::url_encode(number_.value()) + "";

        std::string query_string;

        std::string body_str;
        std::string content_type;

        auto result = client_.execute("DELETE", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return BatchDeleteResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<BatchDeleteResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> number_;
    RequestConfig request_config_;
};

class BatchesClient::UpdateBuilder {
public:
    explicit UpdateBuilder(TofuPilot& client) noexcept : client_(client) {}

    UpdateBuilder& number(std::string value) {
        number_ = std::move(value);
        return *this;
    }
    UpdateBuilder& new_number(std::string value) {
        new_number_ = std::move(value);
        return *this;
    }
    UpdateBuilder& body(BatchUpdateRequestBody b) {
        new_number_ = std::move(b.new_number);
        return *this;
    }

    UpdateBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    UpdateBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    BatchUpdateResponse send() {
        if (!number_.has_value()) throw ValidationError("missing required path parameter: number");

        std::string path = "/v2/batches/" + detail::url_encode(number_.value()) + "";

        std::string query_string;

        std::string body_str;
        std::string content_type;
        {
            BatchUpdateRequestBody req_body;
            if (!new_number_.has_value()) throw ValidationError("missing required field: new_number");
            req_body.new_number = new_number_.value();
            body_str = nlohmann::json(req_body).dump();
            content_type = "application/json";
        }

        auto result = client_.execute("PATCH", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return BatchUpdateResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<BatchUpdateResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> number_;
    std::optional<std::string> new_number_;
    RequestConfig request_config_;
};

class BatchesClient::ListBuilder {
public:
    explicit ListBuilder(TofuPilot& client) noexcept : client_(client) {}

    ListBuilder& ids(std::vector<std::string> value) {
        ids_ = std::move(value);
        return *this;
    }
    ListBuilder& numbers(std::vector<std::string> value) {
        numbers_ = std::move(value);
        return *this;
    }
    ListBuilder& created_after(std::string value) {
        created_after_ = std::move(value);
        return *this;
    }
    ListBuilder& created_before(std::string value) {
        created_before_ = std::move(value);
        return *this;
    }
    ListBuilder& limit(int64_t value) {
        limit_ = std::move(value);
        return *this;
    }
    ListBuilder& cursor(int64_t value) {
        cursor_ = std::move(value);
        return *this;
    }
    ListBuilder& search_query(std::string value) {
        search_query_ = std::move(value);
        return *this;
    }
    ListBuilder& part_numbers(std::vector<std::string> value) {
        part_numbers_ = std::move(value);
        return *this;
    }
    ListBuilder& revision_numbers(std::vector<std::string> value) {
        revision_numbers_ = std::move(value);
        return *this;
    }
    ListBuilder& sort_by(BatchListSortBy value) {
        sort_by_ = std::move(value);
        return *this;
    }
    ListBuilder& sort_order(ListSortOrder value) {
        sort_order_ = std::move(value);
        return *this;
    }

    ListBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    ListBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    BatchListResponse send() {

        std::string path = "/v2/batches";

        std::string query_string;
        {
            std::ostringstream qs;
            bool first = true;
            if (ids_.has_value()) {
                for (const auto& item : ids_.value()) {
                    if (!first) qs << "&";
                    qs << "ids=" << detail::url_encode(detail::to_query_string(item));
                    first = false;
                }
            }
            if (numbers_.has_value()) {
                for (const auto& item : numbers_.value()) {
                    if (!first) qs << "&";
                    qs << "numbers=" << detail::url_encode(detail::to_query_string(item));
                    first = false;
                }
            }
            if (created_after_.has_value()) {
                if (!first) qs << "&";
                qs << "created_after=" << detail::url_encode(detail::to_query_string(created_after_.value()));
                first = false;
            }
            if (created_before_.has_value()) {
                if (!first) qs << "&";
                qs << "created_before=" << detail::url_encode(detail::to_query_string(created_before_.value()));
                first = false;
            }
            if (limit_.has_value()) {
                if (!first) qs << "&";
                qs << "limit=" << detail::url_encode(detail::to_query_string(limit_.value()));
                first = false;
            }
            if (cursor_.has_value()) {
                if (!first) qs << "&";
                qs << "cursor=" << detail::url_encode(detail::to_query_string(cursor_.value()));
                first = false;
            }
            if (search_query_.has_value()) {
                if (!first) qs << "&";
                qs << "search_query=" << detail::url_encode(detail::to_query_string(search_query_.value()));
                first = false;
            }
            if (part_numbers_.has_value()) {
                for (const auto& item : part_numbers_.value()) {
                    if (!first) qs << "&";
                    qs << "part_numbers=" << detail::url_encode(detail::to_query_string(item));
                    first = false;
                }
            }
            if (revision_numbers_.has_value()) {
                for (const auto& item : revision_numbers_.value()) {
                    if (!first) qs << "&";
                    qs << "revision_numbers=" << detail::url_encode(detail::to_query_string(item));
                    first = false;
                }
            }
            if (sort_by_.has_value()) {
                if (!first) qs << "&";
                qs << "sort_by=" << detail::url_encode(detail::to_query_string(sort_by_.value()));
                first = false;
            }
            if (sort_order_.has_value()) {
                if (!first) qs << "&";
                qs << "sort_order=" << detail::url_encode(detail::to_query_string(sort_order_.value()));
                first = false;
            }
            query_string = qs.str();
        }

        std::string body_str;
        std::string content_type;

        auto result = client_.execute("GET", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return BatchListResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<BatchListResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::vector<std::string>> ids_;
    std::optional<std::vector<std::string>> numbers_;
    std::optional<std::string> created_after_;
    std::optional<std::string> created_before_;
    std::optional<int64_t> limit_;
    std::optional<int64_t> cursor_;
    std::optional<std::string> search_query_;
    std::optional<std::vector<std::string>> part_numbers_;
    std::optional<std::vector<std::string>> revision_numbers_;
    std::optional<BatchListSortBy> sort_by_;
    std::optional<ListSortOrder> sort_order_;
    RequestConfig request_config_;
};

class BatchesClient::CreateBuilder {
public:
    explicit CreateBuilder(TofuPilot& client) noexcept : client_(client) {}

    CreateBuilder& number(std::string value) {
        number_ = std::move(value);
        return *this;
    }
    CreateBuilder& body(BatchCreateRequest b) {
        number_ = std::move(b.number);
        return *this;
    }

    CreateBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    CreateBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    BatchCreateResponse send() {

        std::string path = "/v2/batches";

        std::string query_string;

        std::string body_str;
        std::string content_type;
        {
            BatchCreateRequest req_body;
            if (!number_.has_value()) throw ValidationError("missing required field: number");
            req_body.number = number_.value();
            body_str = nlohmann::json(req_body).dump();
            content_type = "application/json";
        }

        auto result = client_.execute("POST", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return BatchCreateResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<BatchCreateResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> number_;
    RequestConfig request_config_;
};

inline BatchesClient::GetBuilder BatchesClient::get() {
    return GetBuilder(client_);
}
inline BatchesClient::DeleteBuilder BatchesClient::delete_() {
    return DeleteBuilder(client_);
}
inline BatchesClient::UpdateBuilder BatchesClient::update() {
    return UpdateBuilder(client_);
}
inline BatchesClient::ListBuilder BatchesClient::list() {
    return ListBuilder(client_);
}
inline BatchesClient::CreateBuilder BatchesClient::create() {
    return CreateBuilder(client_);
}

} // namespace tofupilot
