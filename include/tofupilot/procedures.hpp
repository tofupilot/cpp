// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <chrono>
#include <optional>
#include <sstream>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "config.hpp"
#include "detail.hpp"
#include "error.hpp"
#include "types.hpp"

namespace tofupilot {

class TofuPilot;

class ProceduresClient {
public:
    explicit ProceduresClient(TofuPilot& client) noexcept : client_(client) {}

    class ListBuilder;
    ListBuilder list();
    class CreateBuilder;
    CreateBuilder create();
    class GetBuilder;
    GetBuilder get();
    class DeleteBuilder;
    DeleteBuilder delete_();
    class UpdateBuilder;
    UpdateBuilder update();

private:
    TofuPilot& client_;
};

class ProceduresClient::ListBuilder {
public:
    explicit ListBuilder(TofuPilot& client) noexcept : client_(client) {}

    ListBuilder& limit(int64_t value) {
        limit_ = std::move(value);
        return *this;
    }
    ListBuilder& cursor(double value) {
        cursor_ = std::move(value);
        return *this;
    }
    ListBuilder& search_query(std::string value) {
        search_query_ = std::move(value);
        return *this;
    }
    ListBuilder& created_after(std::string value) {
        created_after_ = std::move(value);
        return *this;
    }
    ListBuilder& created_before(std::string value) {
        created_before_ = std::move(value);
        return *this;
    }

    ListBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    ListBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    ProcedureListResponse send() {

        std::string path = "/v2/procedures";

        std::string query_string;
        {
            std::ostringstream qs;
            bool first = true;
            if (limit_.has_value()) {
                if (!first) qs << "&";
                qs << "limit=" << detail::url_encode(detail::to_query_string(limit_.value()));
                first = false;
            }
            if (cursor_.has_value()) {
                if (!first) qs << "&";
                qs << "cursor=" << detail::url_encode(detail::to_query_string(cursor_.value()));
                first = false;
            }
            if (search_query_.has_value()) {
                if (!first) qs << "&";
                qs << "search_query=" << detail::url_encode(detail::to_query_string(search_query_.value()));
                first = false;
            }
            if (created_after_.has_value()) {
                if (!first) qs << "&";
                qs << "created_after=" << detail::url_encode(detail::to_query_string(created_after_.value()));
                first = false;
            }
            if (created_before_.has_value()) {
                if (!first) qs << "&";
                qs << "created_before=" << detail::url_encode(detail::to_query_string(created_before_.value()));
                first = false;
            }
            query_string = qs.str();
        }

        std::string body_str;
        std::string content_type;

        auto result = client_.execute("GET", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return ProcedureListResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<ProcedureListResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<int64_t> limit_;
    std::optional<double> cursor_;
    std::optional<std::string> search_query_;
    std::optional<std::string> created_after_;
    std::optional<std::string> created_before_;
    RequestConfig request_config_;
};

class ProceduresClient::CreateBuilder {
public:
    explicit CreateBuilder(TofuPilot& client) noexcept : client_(client) {}

    CreateBuilder& name(std::string value) {
        name_ = std::move(value);
        return *this;
    }
    CreateBuilder& body(ProcedureCreateRequest b) {
        name_ = std::move(b.name);
        return *this;
    }

    CreateBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    CreateBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    ProcedureCreateResponse send() {

        std::string path = "/v2/procedures";

        std::string query_string;

        std::string body_str;
        std::string content_type;
        {
            ProcedureCreateRequest req_body;
            if (!name_.has_value()) throw ValidationError("missing required field: name");
            req_body.name = name_.value();
            body_str = nlohmann::json(req_body).dump();
            content_type = "application/json";
        }

        auto result = client_.execute("POST", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return ProcedureCreateResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<ProcedureCreateResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> name_;
    RequestConfig request_config_;
};

class ProceduresClient::GetBuilder {
public:
    explicit GetBuilder(TofuPilot& client) noexcept : client_(client) {}

    GetBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    GetBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    GetBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    ProcedureGetResponse send() {
        if (!id_.has_value()) throw ValidationError("missing required path parameter: id");

        std::string path = "/v2/procedures/" + detail::url_encode(id_.value()) + "";

        std::string query_string;

        std::string body_str;
        std::string content_type;

        auto result = client_.execute("GET", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return ProcedureGetResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<ProcedureGetResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> id_;
    RequestConfig request_config_;
};

class ProceduresClient::DeleteBuilder {
public:
    explicit DeleteBuilder(TofuPilot& client) noexcept : client_(client) {}

    DeleteBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }

    DeleteBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    DeleteBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    ProcedureDeleteResponse send() {
        if (!id_.has_value()) throw ValidationError("missing required path parameter: id");

        std::string path = "/v2/procedures/" + detail::url_encode(id_.value()) + "";

        std::string query_string;

        std::string body_str;
        std::string content_type;

        auto result = client_.execute("DELETE", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return ProcedureDeleteResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<ProcedureDeleteResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> id_;
    RequestConfig request_config_;
};

class ProceduresClient::UpdateBuilder {
public:
    explicit UpdateBuilder(TofuPilot& client) noexcept : client_(client) {}

    UpdateBuilder& id(std::string value) {
        id_ = std::move(value);
        return *this;
    }
    UpdateBuilder& name(std::string value) {
        name_ = std::move(value);
        return *this;
    }
    UpdateBuilder& body(ProcedureUpdateRequestBody b) {
        name_ = std::move(b.name);
        return *this;
    }

    UpdateBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    UpdateBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    ProcedureUpdateResponse send() {
        if (!id_.has_value()) throw ValidationError("missing required path parameter: id");

        std::string path = "/v2/procedures/" + detail::url_encode(id_.value()) + "";

        std::string query_string;

        std::string body_str;
        std::string content_type;
        {
            ProcedureUpdateRequestBody req_body;
            if (!name_.has_value()) throw ValidationError("missing required field: name");
            req_body.name = name_.value();
            body_str = nlohmann::json(req_body).dump();
            content_type = "application/json";
        }

        auto result = client_.execute("PATCH", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return ProcedureUpdateResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<ProcedureUpdateResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> id_;
    std::optional<std::string> name_;
    RequestConfig request_config_;
};

inline ProceduresClient::ListBuilder ProceduresClient::list() {
    return ListBuilder(client_);
}
inline ProceduresClient::CreateBuilder ProceduresClient::create() {
    return CreateBuilder(client_);
}
inline ProceduresClient::GetBuilder ProceduresClient::get() {
    return GetBuilder(client_);
}
inline ProceduresClient::DeleteBuilder ProceduresClient::delete_() {
    return DeleteBuilder(client_);
}
inline ProceduresClient::UpdateBuilder ProceduresClient::update() {
    return UpdateBuilder(client_);
}

} // namespace tofupilot
