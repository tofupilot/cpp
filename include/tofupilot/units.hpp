// Auto-generated by TofuPilot SDK Generator. DO NOT EDIT.

#pragma once

#include <chrono>
#include <optional>
#include <sstream>
#include <string>
#include <utility>
#include <vector>

#include <nlohmann/json.hpp>

#include "config.hpp"
#include "detail.hpp"
#include "error.hpp"
#include "types.hpp"

namespace tofupilot {

class TofuPilot;

class UnitsClient {
public:
    explicit UnitsClient(TofuPilot& client) noexcept : client_(client) {}

    class ListBuilder;
    ListBuilder list();
    class CreateBuilder;
    CreateBuilder create();
    class DeleteBuilder;
    DeleteBuilder delete_();
    class GetBuilder;
    GetBuilder get();
    class UpdateBuilder;
    UpdateBuilder update();
    class AddChildBuilder;
    AddChildBuilder add_child();
    class RemoveChildBuilder;
    RemoveChildBuilder remove_child();

private:
    TofuPilot& client_;
};

class UnitsClient::ListBuilder {
public:
    explicit ListBuilder(TofuPilot& client) noexcept : client_(client) {}

    ListBuilder& search_query(std::string value) {
        search_query_ = std::move(value);
        return *this;
    }
    ListBuilder& ids(std::vector<std::string> value) {
        ids_ = std::move(value);
        return *this;
    }
    ListBuilder& serial_numbers(std::vector<std::string> value) {
        serial_numbers_ = std::move(value);
        return *this;
    }
    ListBuilder& part_numbers(std::vector<std::string> value) {
        part_numbers_ = std::move(value);
        return *this;
    }
    ListBuilder& revision_numbers(std::vector<std::string> value) {
        revision_numbers_ = std::move(value);
        return *this;
    }
    ListBuilder& batch_numbers(std::vector<std::string> value) {
        batch_numbers_ = std::move(value);
        return *this;
    }
    ListBuilder& procedure_ids(std::vector<std::string> value) {
        procedure_ids_ = std::move(value);
        return *this;
    }
    ListBuilder& outcomes(std::vector<Outcome> value) {
        outcomes_ = std::move(value);
        return *this;
    }
    ListBuilder& started_after(std::string value) {
        started_after_ = std::move(value);
        return *this;
    }
    ListBuilder& started_before(std::string value) {
        started_before_ = std::move(value);
        return *this;
    }
    ListBuilder& latest_only(bool value) {
        latest_only_ = std::move(value);
        return *this;
    }
    ListBuilder& run_count_min(int64_t value) {
        run_count_min_ = std::move(value);
        return *this;
    }
    ListBuilder& run_count_max(int64_t value) {
        run_count_max_ = std::move(value);
        return *this;
    }
    ListBuilder& created_after(std::string value) {
        created_after_ = std::move(value);
        return *this;
    }
    ListBuilder& created_before(std::string value) {
        created_before_ = std::move(value);
        return *this;
    }
    ListBuilder& created_by_user_ids(std::vector<std::string> value) {
        created_by_user_ids_ = std::move(value);
        return *this;
    }
    ListBuilder& created_by_station_ids(std::vector<std::string> value) {
        created_by_station_ids_ = std::move(value);
        return *this;
    }
    ListBuilder& exclude_units_with_parent(bool value) {
        exclude_units_with_parent_ = std::move(value);
        return *this;
    }
    ListBuilder& limit(int64_t value) {
        limit_ = std::move(value);
        return *this;
    }
    ListBuilder& cursor(int64_t value) {
        cursor_ = std::move(value);
        return *this;
    }
    ListBuilder& sort_by(UnitListSortBy value) {
        sort_by_ = std::move(value);
        return *this;
    }
    ListBuilder& sort_order(ListSortOrder value) {
        sort_order_ = std::move(value);
        return *this;
    }

    ListBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    ListBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    UnitListResponse send() {

        std::string path = "/v2/units";

        std::string query_string;
        {
            std::ostringstream qs;
            bool first = true;
            if (search_query_.has_value()) {
                if (!first) qs << "&";
                qs << "search_query=" << detail::url_encode(detail::to_query_string(search_query_.value()));
                first = false;
            }
            if (ids_.has_value()) {
                for (const auto& item : ids_.value()) {
                    if (!first) qs << "&";
                    qs << "ids=" << detail::url_encode(detail::to_query_string(item));
                    first = false;
                }
            }
            if (serial_numbers_.has_value()) {
                for (const auto& item : serial_numbers_.value()) {
                    if (!first) qs << "&";
                    qs << "serial_numbers=" << detail::url_encode(detail::to_query_string(item));
                    first = false;
                }
            }
            if (part_numbers_.has_value()) {
                for (const auto& item : part_numbers_.value()) {
                    if (!first) qs << "&";
                    qs << "part_numbers=" << detail::url_encode(detail::to_query_string(item));
                    first = false;
                }
            }
            if (revision_numbers_.has_value()) {
                for (const auto& item : revision_numbers_.value()) {
                    if (!first) qs << "&";
                    qs << "revision_numbers=" << detail::url_encode(detail::to_query_string(item));
                    first = false;
                }
            }
            if (batch_numbers_.has_value()) {
                for (const auto& item : batch_numbers_.value()) {
                    if (!first) qs << "&";
                    qs << "batch_numbers=" << detail::url_encode(detail::to_query_string(item));
                    first = false;
                }
            }
            if (procedure_ids_.has_value()) {
                for (const auto& item : procedure_ids_.value()) {
                    if (!first) qs << "&";
                    qs << "procedure_ids=" << detail::url_encode(detail::to_query_string(item));
                    first = false;
                }
            }
            if (outcomes_.has_value()) {
                for (const auto& item : outcomes_.value()) {
                    if (!first) qs << "&";
                    qs << "outcomes=" << detail::url_encode(detail::to_query_string(item));
                    first = false;
                }
            }
            if (started_after_.has_value()) {
                if (!first) qs << "&";
                qs << "started_after=" << detail::url_encode(detail::to_query_string(started_after_.value()));
                first = false;
            }
            if (started_before_.has_value()) {
                if (!first) qs << "&";
                qs << "started_before=" << detail::url_encode(detail::to_query_string(started_before_.value()));
                first = false;
            }
            if (latest_only_.has_value()) {
                if (!first) qs << "&";
                qs << "latest_only=" << detail::url_encode(detail::to_query_string(latest_only_.value()));
                first = false;
            }
            if (run_count_min_.has_value()) {
                if (!first) qs << "&";
                qs << "run_count_min=" << detail::url_encode(detail::to_query_string(run_count_min_.value()));
                first = false;
            }
            if (run_count_max_.has_value()) {
                if (!first) qs << "&";
                qs << "run_count_max=" << detail::url_encode(detail::to_query_string(run_count_max_.value()));
                first = false;
            }
            if (created_after_.has_value()) {
                if (!first) qs << "&";
                qs << "created_after=" << detail::url_encode(detail::to_query_string(created_after_.value()));
                first = false;
            }
            if (created_before_.has_value()) {
                if (!first) qs << "&";
                qs << "created_before=" << detail::url_encode(detail::to_query_string(created_before_.value()));
                first = false;
            }
            if (created_by_user_ids_.has_value()) {
                for (const auto& item : created_by_user_ids_.value()) {
                    if (!first) qs << "&";
                    qs << "created_by_user_ids=" << detail::url_encode(detail::to_query_string(item));
                    first = false;
                }
            }
            if (created_by_station_ids_.has_value()) {
                for (const auto& item : created_by_station_ids_.value()) {
                    if (!first) qs << "&";
                    qs << "created_by_station_ids=" << detail::url_encode(detail::to_query_string(item));
                    first = false;
                }
            }
            if (exclude_units_with_parent_.has_value()) {
                if (!first) qs << "&";
                qs << "exclude_units_with_parent=" << detail::url_encode(detail::to_query_string(exclude_units_with_parent_.value()));
                first = false;
            }
            if (limit_.has_value()) {
                if (!first) qs << "&";
                qs << "limit=" << detail::url_encode(detail::to_query_string(limit_.value()));
                first = false;
            }
            if (cursor_.has_value()) {
                if (!first) qs << "&";
                qs << "cursor=" << detail::url_encode(detail::to_query_string(cursor_.value()));
                first = false;
            }
            if (sort_by_.has_value()) {
                if (!first) qs << "&";
                qs << "sort_by=" << detail::url_encode(detail::to_query_string(sort_by_.value()));
                first = false;
            }
            if (sort_order_.has_value()) {
                if (!first) qs << "&";
                qs << "sort_order=" << detail::url_encode(detail::to_query_string(sort_order_.value()));
                first = false;
            }
            query_string = qs.str();
        }

        std::string body_str;
        std::string content_type;

        auto result = client_.execute("GET", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return UnitListResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<UnitListResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> search_query_;
    std::optional<std::vector<std::string>> ids_;
    std::optional<std::vector<std::string>> serial_numbers_;
    std::optional<std::vector<std::string>> part_numbers_;
    std::optional<std::vector<std::string>> revision_numbers_;
    std::optional<std::vector<std::string>> batch_numbers_;
    std::optional<std::vector<std::string>> procedure_ids_;
    std::optional<std::vector<Outcome>> outcomes_;
    std::optional<std::string> started_after_;
    std::optional<std::string> started_before_;
    std::optional<bool> latest_only_;
    std::optional<int64_t> run_count_min_;
    std::optional<int64_t> run_count_max_;
    std::optional<std::string> created_after_;
    std::optional<std::string> created_before_;
    std::optional<std::vector<std::string>> created_by_user_ids_;
    std::optional<std::vector<std::string>> created_by_station_ids_;
    std::optional<bool> exclude_units_with_parent_;
    std::optional<int64_t> limit_;
    std::optional<int64_t> cursor_;
    std::optional<UnitListSortBy> sort_by_;
    std::optional<ListSortOrder> sort_order_;
    RequestConfig request_config_;
};

class UnitsClient::CreateBuilder {
public:
    explicit CreateBuilder(TofuPilot& client) noexcept : client_(client) {}

    CreateBuilder& serial_number(std::string value) {
        serial_number_ = std::move(value);
        return *this;
    }
    CreateBuilder& part_number(std::string value) {
        part_number_ = std::move(value);
        return *this;
    }
    CreateBuilder& revision_number(std::string value) {
        revision_number_ = std::move(value);
        return *this;
    }
    CreateBuilder& body(UnitCreateRequest b) {
        serial_number_ = std::move(b.serial_number);
        part_number_ = std::move(b.part_number);
        revision_number_ = std::move(b.revision_number);
        return *this;
    }

    CreateBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    CreateBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    UnitCreateResponse send() {

        std::string path = "/v2/units";

        std::string query_string;

        std::string body_str;
        std::string content_type;
        {
            UnitCreateRequest req_body;
            if (!serial_number_.has_value()) throw ValidationError("missing required field: serial_number");
            req_body.serial_number = serial_number_.value();
            if (!part_number_.has_value()) throw ValidationError("missing required field: part_number");
            req_body.part_number = part_number_.value();
            if (!revision_number_.has_value()) throw ValidationError("missing required field: revision_number");
            req_body.revision_number = revision_number_.value();
            body_str = nlohmann::json(req_body).dump();
            content_type = "application/json";
        }

        auto result = client_.execute("POST", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return UnitCreateResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<UnitCreateResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> serial_number_;
    std::optional<std::string> part_number_;
    std::optional<std::string> revision_number_;
    RequestConfig request_config_;
};

class UnitsClient::DeleteBuilder {
public:
    explicit DeleteBuilder(TofuPilot& client) noexcept : client_(client) {}

    DeleteBuilder& serial_numbers(std::vector<std::string> value) {
        serial_numbers_ = std::move(value);
        return *this;
    }

    DeleteBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    DeleteBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    UnitDeleteResponse send() {

        std::string path = "/v2/units";

        std::string query_string;
        {
            std::ostringstream qs;
            bool first = true;
            if (serial_numbers_.has_value()) {
                for (const auto& item : serial_numbers_.value()) {
                    if (!first) qs << "&";
                    qs << "serial_numbers=" << detail::url_encode(detail::to_query_string(item));
                    first = false;
                }
            }
            query_string = qs.str();
        }

        std::string body_str;
        std::string content_type;

        auto result = client_.execute("DELETE", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return UnitDeleteResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<UnitDeleteResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::vector<std::string>> serial_numbers_;
    RequestConfig request_config_;
};

class UnitsClient::GetBuilder {
public:
    explicit GetBuilder(TofuPilot& client) noexcept : client_(client) {}

    GetBuilder& serial_number(std::string value) {
        serial_number_ = std::move(value);
        return *this;
    }

    GetBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    GetBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    UnitGetResponse send() {
        if (!serial_number_.has_value()) throw ValidationError("missing required path parameter: serial_number");

        std::string path = "/v2/units/" + detail::url_encode(serial_number_.value()) + "";

        std::string query_string;

        std::string body_str;
        std::string content_type;

        auto result = client_.execute("GET", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return UnitGetResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<UnitGetResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> serial_number_;
    RequestConfig request_config_;
};

class UnitsClient::UpdateBuilder {
public:
    explicit UpdateBuilder(TofuPilot& client) noexcept : client_(client) {}

    UpdateBuilder& serial_number(std::string value) {
        serial_number_ = std::move(value);
        return *this;
    }
    UpdateBuilder& new_serial_number(std::string value) {
        new_serial_number_ = std::move(value);
        return *this;
    }
    UpdateBuilder& part_number(std::string value) {
        part_number_ = std::move(value);
        return *this;
    }
    UpdateBuilder& revision_number(std::string value) {
        revision_number_ = std::move(value);
        return *this;
    }
    UpdateBuilder& batch_number(std::string value) {
        batch_number_ = NullableField<std::string>::value(std::move(value));
        return *this;
    }
    UpdateBuilder& batch_number_null() {
        batch_number_ = NullableField<std::string>::make_null();
        return *this;
    }
    UpdateBuilder& attachments(std::vector<std::string> value) {
        attachments_ = std::move(value);
        return *this;
    }
    UpdateBuilder& body(UnitUpdateRequestBody b) {
        if (b.new_serial_number.has_value()) new_serial_number_ = std::move(b.new_serial_number);
        if (b.part_number.has_value()) part_number_ = std::move(b.part_number);
        if (b.revision_number.has_value()) revision_number_ = std::move(b.revision_number);
        batch_number_ = std::move(b.batch_number);
        if (b.attachments.has_value()) attachments_ = std::move(b.attachments);
        return *this;
    }

    UpdateBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    UpdateBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    UnitUpdateResponse send() {
        if (!serial_number_.has_value()) throw ValidationError("missing required path parameter: serial_number");

        std::string path = "/v2/units/" + detail::url_encode(serial_number_.value()) + "";

        std::string query_string;

        std::string body_str;
        std::string content_type;
        {
            UnitUpdateRequestBody req_body;
            req_body.new_serial_number = new_serial_number_;
            req_body.part_number = part_number_;
            req_body.revision_number = revision_number_;
            if (!batch_number_.is_absent()) req_body.batch_number = batch_number_;
            req_body.attachments = attachments_;
            body_str = nlohmann::json(req_body).dump();
            content_type = "application/json";
        }

        auto result = client_.execute("PATCH", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return UnitUpdateResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<UnitUpdateResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> serial_number_;
    std::optional<std::string> new_serial_number_;
    std::optional<std::string> part_number_;
    std::optional<std::string> revision_number_;
    NullableField<std::string> batch_number_;
    std::optional<std::vector<std::string>> attachments_;
    RequestConfig request_config_;
};

class UnitsClient::AddChildBuilder {
public:
    explicit AddChildBuilder(TofuPilot& client) noexcept : client_(client) {}

    AddChildBuilder& serial_number(std::string value) {
        serial_number_ = std::move(value);
        return *this;
    }
    AddChildBuilder& child_serial_number(std::string value) {
        child_serial_number_ = std::move(value);
        return *this;
    }
    AddChildBuilder& body(UnitAddChildRequestBody b) {
        child_serial_number_ = std::move(b.child_serial_number);
        return *this;
    }

    AddChildBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    AddChildBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    UnitAddChildResponse send() {
        if (!serial_number_.has_value()) throw ValidationError("missing required path parameter: serial_number");

        std::string path = "/v2/units/" + detail::url_encode(serial_number_.value()) + "/children";

        std::string query_string;

        std::string body_str;
        std::string content_type;
        {
            UnitAddChildRequestBody req_body;
            if (!child_serial_number_.has_value()) throw ValidationError("missing required field: child_serial_number");
            req_body.child_serial_number = child_serial_number_.value();
            body_str = nlohmann::json(req_body).dump();
            content_type = "application/json";
        }

        auto result = client_.execute("PUT", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return UnitAddChildResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<UnitAddChildResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> serial_number_;
    std::optional<std::string> child_serial_number_;
    RequestConfig request_config_;
};

class UnitsClient::RemoveChildBuilder {
public:
    explicit RemoveChildBuilder(TofuPilot& client) noexcept : client_(client) {}

    RemoveChildBuilder& serial_number(std::string value) {
        serial_number_ = std::move(value);
        return *this;
    }
    RemoveChildBuilder& child_serial_number(std::string value) {
        child_serial_number_ = std::move(value);
        return *this;
    }

    RemoveChildBuilder& server_url(std::string url) {
        request_config_.server_url = std::move(url);
        return *this;
    }
    RemoveChildBuilder& timeout(std::chrono::seconds t) {
        request_config_.timeout = t;
        return *this;
    }

    UnitRemoveChildResponse send() {
        if (!serial_number_.has_value()) throw ValidationError("missing required path parameter: serial_number");

        std::string path = "/v2/units/" + detail::url_encode(serial_number_.value()) + "/children";

        std::string query_string;
        {
            std::ostringstream qs;
            bool first = true;
            if (child_serial_number_.has_value()) {
                if (!first) qs << "&";
                qs << "child_serial_number=" << detail::url_encode(detail::to_query_string(child_serial_number_.value()));
                first = false;
            }
            query_string = qs.str();
        }

        std::string body_str;
        std::string content_type;

        auto result = client_.execute("DELETE", path, body_str, content_type, query_string,
            request_config_.is_default() ? nullptr : &request_config_);

        const auto& resp_body = result->body;
        if (resp_body.empty()) {
            return UnitRemoveChildResponse{};
        }
        try {
            return nlohmann::json::parse(resp_body).get<UnitRemoveChildResponse>();
        } catch (const nlohmann::json::parse_error& e) {
            throw HttpError(std::string("Invalid JSON in response: ") + e.what());
        }
    }

private:
    TofuPilot& client_;
    std::optional<std::string> serial_number_;
    std::optional<std::string> child_serial_number_;
    RequestConfig request_config_;
};

inline UnitsClient::ListBuilder UnitsClient::list() {
    return ListBuilder(client_);
}
inline UnitsClient::CreateBuilder UnitsClient::create() {
    return CreateBuilder(client_);
}
inline UnitsClient::DeleteBuilder UnitsClient::delete_() {
    return DeleteBuilder(client_);
}
inline UnitsClient::GetBuilder UnitsClient::get() {
    return GetBuilder(client_);
}
inline UnitsClient::UpdateBuilder UnitsClient::update() {
    return UpdateBuilder(client_);
}
inline UnitsClient::AddChildBuilder UnitsClient::add_child() {
    return AddChildBuilder(client_);
}
inline UnitsClient::RemoveChildBuilder UnitsClient::remove_child() {
    return RemoveChildBuilder(client_);
}

} // namespace tofupilot
